# The legacy shadow SQLite (sqlite4java) has no FTS5 module. NATIVE mode links the real
# Android SQLite amalgamation, which does — required for the FTS5 external-content table
# (technical design §12.1).
sqliteMode=NATIVE
