# UI conformance register

Phase C of [`spec/ui-conformance-plan.md`](../../spec/ui-conformance-plan.md). Every finding is
one row; every row is assigned to exactly one work package. This is the only place findings live.
Builders close rows; validators add rows; the lead confirms or rejects.

**Evidence.** Emulator screenshots taken 2026-09-07 on a Pixel 6 / API 34 AVD (`ort_audit`,
port 5554) of the app at `6aaa608`, and the source under `app/src/main/kotlin/org/ort/app/` at
`bff688b` (which includes audit fixes F-002, F-004, F-017, F-018, F-019 merged during phase A).
Each row names the artboard it was compared against; the artboards are under
[`design/canvas/`](../../design/canvas/) and the tokens in [`design/design-guide.md`](../../design/design-guide.md).

**Severity.** `halt` — wrong or misleading to the operator (constitution I) · `spec` — an
FR / P / F not met · `design` — the artboard not matched · `polish`.

**Status.** `open` · `building` (a builder owns it) · `fixed` (builder reports it closed, lead
not yet confirmed) · `closed` (confirmed on the emulator) · `rejected` (lead disagrees; reason
in the row).

---

## Theme, manifest, scaffold — WP1

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-001 | every | any phone board | No `android:theme` in the manifest, so every Activity renders a platform `DeviceDefault` ActionBar titled "Offline Radio Transcriber" *above* the Compose top bar. Every screen shows two headers. Screenshot `03-now.png`. | halt | closed |
| R-002 | launch | `Setup-Mic` | `MainActivity` is an unthemed `TextView` reading "scaffold running… Requesting microphone access…" on the platform ground, then fires bare OS prompts. Screenshot `01-launch.png`. | design | closed |
| R-003 | every drawer destination | `Main`, `Log` headers | The Compose `TopAppBar` carries the destination label as a title, and the screen body repeats it 27px below. "Now" / "Log" / "Search" each appear twice. The artboard header is icons only — drawer, live dot + elapsed, search — with no title text. | design | fixed |
| R-004 | every | `Icons` | The drawer control is the text glyph `=` in body type. The artboard is the 21px three-line stroke icon at `text/icon` in a 44px target. Same for search: absent. | design | fixed |
| R-005 | every | `Type` | `OrtType.typography` maps M3 `bodyMedium` to the 12.5px caption and `bodyLarge` to 15px, so every screen that asks for `MaterialTheme.typography.bodyMedium` gets caption size for body copy. The ramp has no 14 / 14.5 / 11.5 / 17px roles, which the boards use for controls, row titles, sub-lines and figures. Platform fallback faces are correct per constitution V; weights and tracking must still match §4 of the guide. | design | fixed |
| R-006 | every | `Tokens` | `OrtTheme` sets a colour scheme but no root `Surface`; the window ground is whatever `DeviceDefault` gives. Screens must sit on `bg/screen` `oklch(0.155 0.008 250)` (≈ `#202329`) from the theme, not from the OS. | design | closed |
| R-007 | every | — | `ReaderActivity` is `exported="false"` and `MainActivity` `finish()`es after launching it; the back stack after a cold start is `Reader` alone, which is right — but a process death mid-session relaunches into `MainActivity`'s permission flow rather than the reader. Verify and route. | polish | fixed |
| R-008 | every | guide §12 | Found on the emulator after WP1 merged (`8ad7cc1`): status-bar clock and icons render dark on the dark ground on both activities — `enableEdgeToEdge()` with no arguments chooses icon colour from the OS night-mode setting, and this app is dark-only. Both activities must pass `SystemBarStyle.dark(TRANSPARENT)` for status and navigation bars, and `themes.xml` must set `windowLightStatusBar`/`windowLightNavigationBar` false. Screenshot `wp1-02-now.png`. | design | closed |

## Drawer and navigation — WP3

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-010 | drawer | `Menu` | No icons, no session header ("Repeater watch / TH-D75A · both bands"), no divider before Capture/Improve/Settings (`ReaderDestination.trailingGroup` exists and is never read), no selected-row pill on `bg/selected`. Counts: F-020 added a Log count and Capture elapsed badge and renders Threads as "—" (deliberate — nothing populates `threadId`); Stations and Frequencies counts are still absent and the badge is plain `bodySmall` text, not the mono 11.5px `text/figure` the board specifies. Screenshot `05-drawer.png` predates F-020. | design | fixed |
| R-011 | drawer | `Grid` | Rows measure ≈31dp tall (8dp padding + one text line). Guide §5 and FR-A11Y-2: 44dp minimum, whole row is the target. | halt | fixed |
| R-012 | drawer footer | `Menu` footer, D26 | **Partly fixed by F-020** (merged during phase A): the footer now reads the audio directory's real usage and says "no budget set" visibly, so the constitution-I misrepresentation is gone. Remaining: the layout. The board is one baseline row — "Audio 38.2 GB" in `text/dim` 12px left, "of 60" mono 11px `text/figure` right — over a 4px `line/default` track with a `oklch(0.60 0.10 150)` fill, above a `line/default` top border. Built is two stacked `bodyMedium` lines and no bar. When a budget exists (WP10, FR-STO), "of N" is the budget; until then the row reads "Audio 0.8 GB · no budget set" and the bar is omitted, not drawn against device total. | design | fixed |
| R-013 | drawer | `Menu` | The three unbuilt destinations (Earlier nights, Capture, Improve records — Settings became real with F-008) look identical to the seven built ones; `hasScreen` is never rendered. Until WP10/WP11 land, an unbuilt row must say so in the row (`text/disabled`, "not built"), not only after a tap. | design | fixed |
| R-014 | drawer | — | `DrawerRow` uses `selectable` with no `Role` and overrides the merged description with "Open Now", so TalkBack never announces which destination is current. FR-A11Y-2. | spec | fixed |
| R-015 | every drawer destination | `Main` header | No search icon in any header; search is reachable only as a drawer row. `Flow-Search`: the magnifier is top-right on every destination. | design | fixed |
| R-016 | drill-ins | `Detail` header | `TransmissionDetailContent` / `StationDetailContent` / `FrequencyDetailContent` replace the whole scaffold and each screen draws its own "‹ Back" text row. The artboard header is a 20px chevron + the parent destination's label in `accent/green`, 44px target. One header component, not three text rows. | design | fixed |
| R-017 | drill-ins | `Search-Results` → `Detail` | Back from a detail opened from Search returns to the Log destination, not to Search with its filters intact (the `openTransmissionId` model has no origin). `Flow-Search`: back label reads "Search" and restores the results. | spec | fixed |

## Shared components — WP2

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-020 | every attribution | `States`, `Rows`, guide §6.1–6.2 | `AttributionMarker` draws the shape, then a text glyph (`✓` `~` `?` `—`), then the confidence for every state that has one. The artboard is shape + mono callsign, with a score chip **only** on INFERRED and the `or QRF` alternate on AMBIGUOUS; CONFIRMED shows no number. Callers then append the callsign *outside* the marker with a literal `"  "` spacer. The marker must own the callsign and the chip. | design | fixed |
| R-021 | station, frequency | `Charts`, guide §8 | `ActivityPatternChart`: bars are a single `accentGreen` with no intensity ramp (guide: five green steps, neutral for low); 34dp tall with 1dp side padding (board: 38px, 1.5px gap); not-listening is drawn as 2dp `accentAmber` diagonal lines on 5dp (guide: `hatch/bar` `oklch(0.40 0.055 60)` stripe, 2 on 5); no axis labels; the legend is the font glyph `▨` (guide §7: never a font glyph — a 9×7 hatch swatch + "not listening · 38 s"); title is a hardcoded uppercase string "ACTIVITY BY HOUR (UTC)" instead of the section-label style with the guide's copy. | design | fixed |
| R-022 | every running screen | `Main` footer, guide §6.6 | No live bar component exists. P4 and P5 both depend on it: level meter, italic partial, `Live` — pinned to the bottom of every screen while a session runs, and its degraded (amber) and halted (red) variants. | spec | fixed |
| R-023 | many | `Controls`, `Feedback`, guide §6 | No `FilterChip`, `Banner` (amber / red), `Toast` with undo, `Sheet`, `StepIndicator`, `Badge` (NEW / REVISED / CORRECTED / tier), `ScoreChip`, `Tile`, `WaveformCard`, `Radio` / `Checkbox` / `Toggle` rows, `SectionHeader` with trailing action, `EmptyState`, `FailedState`. Every screen hand-rolls text where one of these belongs. | design | fixed |
| R-024 | every text action | `Controls`, guide §6.7 | Actions are `Text` + `clickable` with no pressed state and the text's own height as the target ("Filter", "Search", "▶ Play", "‹ Back", "Save"). Guide: 44px hit area, pressed state on `bg/pressed`, `accent/green` 13px/500. | halt | fixed |
| R-025 | every | guide §7 | Icons: none of the guide's stroke set exists as composables; screens use `=`, `▶`, `‹`, `▸`/`▾`, `▨` glyphs. | design | fixed |

## Now and capture status — WP4

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-030 | Now | `Main` | "Worth knowing" and everything after `StatusScreen` is unreachable: `StatusScreen` is `fillMaxSize()` inside `NowScreen`'s `Column`, which does not scroll. Swiping does nothing. Screenshot `04-now-scrolled.png`. | halt | fixed |
| R-031 | Now | `Capture-Status` | "ASR: ASR: unavailable — …" and "VAD: VAD: energy fallback (…)": `StatusScreen.LabeledLine("ASR", …)` prefixes a value that `StatusViewStateMapper.asrLabel` already prefixes. Screenshot `03-now.png`. | halt | fixed |
| R-032 | Now | `Capture-Status`, FR-UI-7 | Of FR-UI-7's nine required facts, six are absent: input device **and verified route**, rig connection state, current tier (as a tier number, not an ASR sentence), storage used, battery. Present: state, elapsed, transmissions, backlog (as "Not measured" / "N queued"). | spec | fixed |
| R-033 | Now | `Main` | The home is a status field dump. The artboard is: header (drawer · live dot + elapsed · search), session title (`Overnight`, not "Now") + `412 overs · 19 stations · 2 bands`, the hour activity chart with the not-listening hatch and legend, `WORTH KNOWING` digest items with state dots, `STATIONS HEARD` rows with `All 19`, and the live bar. One of six elements is present. The field dump belongs on `Capture-Status`, reached from the drawer's Capture row and from the live bar. | design | fixed |
| R-034 | Now, status | `Feedback` failed state, `Fail-Model`, guide §9 | The ASR/VAD rows print the raw availability reason: an absolute file path, expected filenames, `EnergyVadModel`, "see asr-sherpa/README.md for the fetch URL". Operator surfaces use the *failed* pattern: "No transcription model installed — audio is being captured and kept; every over is transcribed once one is installed — Install a model". The path goes to Diagnostics. | halt | fixed |
| R-035 | drawer › Capture | `Capture-Status` | The Capture destination is a `PlaceholderScreen`. FR-UI-7 says the status surface is always one tap away; today it is reachable only as the body of Now. | spec | fixed |
| R-036 | Now | `Now-Idle`, `Now-First` | No idle state (no session: the artboard is "Not capturing · Last session ended …", a Start capture button, Earlier nights, Can get better) and no first-session state (chart baseline, "Nothing yet — the digest builds as overs arrive", "Listening since 23:32"). Today idle is the word "Idle" inside the field dump. | design | fixed |
| R-037 | Now | `Main` | `NowScreen` hardcodes "Worth knowing / Nothing to report yet." with a content description pointing at `spec/build-plan.md`. Until digest exists (WP10), render the first-session empty state from `Now-First`; never a developer note as operator copy. | design | fixed |
| R-038 | status | `Capture-Status` | Shed level is labelled "Shed level: Nominal / Level N — …". The artboard has no "shed level" — the operator sees Tier (`3 of 3`), Thermal (`Nominal · RTF 0.31`), Backlog (`3 overs waiting · not growing`). Map the shed levels onto those three rows. | design | fixed |
| R-039 | status | `Level-Meter` | No level surface at all (FR-CAP-3, F3). | spec | fixed |

## Log and threads — WP5

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-040 | Log | `Log`, `Rows`, `Grid` | Rows are two free-width columns of captions. The artboard row is `[time 52px mono] [freq 56px mono] [marker + callsign / transcript] [sig mono right]` at `8px 20px 9px` with a `line/row` divider — plus a mono uppercase column-header row above. None of: `Filter` action, filter chips, QSO group header (`bg/group`, "QSO · 4 overs · 2 stations"), gap row (`bg/row-gap`, "not listening · 38 s · incoming call"), rejected row (45% opacity, "rejected · squelch tail"), ambiguous row tint, `NEW` badge, `or QRF` alternate. Screenshot `06-log.png` (empty) + `Rows`. | design | fixed |
| R-041 | Log | `Log-Partial`, `Rows`, FR-UI-1, P5 | No Pass A partial rendering. FR-UI-1: "Pass A partials appear and are visibly replaced by Pass B finals". The artboard has three stages — `hearing…` (mini meter, italic, no marker), `resolving…` (ring, italic), then the marker with a `REVISED` badge if the text changed. `revisionNote` exists in the view state but there is no partial state at all. | spec | fixed |
| R-042 | Log | `Log-Filter`, FR-UI-3 | No filter on the Log. The artboard is a sheet: frequency chips with counts, attribution checkboxes with counts, "also show" rejected/gaps, a time range, `Show N overs`. | spec | fixed |
| R-043 | Log | `Log-Rejected`, P9 | Rejected segments are not reachable from the Log. `TransmissionDetail.rejectionReason` exists; nothing renders it as a row or offers the `Rejected` chip. | spec | fixed |
| R-044 | Threads | `Threads`, `Thread-Detail`, `Threads-Ungrouped`, FR-UI-2 | `ThreadScreen` is a flat list with a section-label per group and a reasoning caption per row. The artboard is: thread cards (kind · frequency · count, participants in mono, meta line), a `Thread-Detail` with the "How these were attributed" card and per-over reasoning lines linking to the source over, and a `Threads-Ungrouped` state that explains *why* (tier) and offers by-frequency meanwhile. | design | fixed |
| R-045 | Log, Threads | `Log-Empty` | Empty states are a bare "No transmissions yet". The artboard keeps the header, chips (dimmed) and column headers, and says "Listening since 23:32 on 145.230 and 146.960. The first one appears here the moment squelch opens." | design | fixed |

## Transmission detail — WP6

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-050 | detail | `Detail`, `Detail-Confirmed` | Layout does not match: built is "‹ Back" row, marker + text label, caption meta, "▶ Play" text, "Transcript" label, disclosure-arrow text rows ("▸ Correct attribution", "▸ Record labelled sample") with raw `TextField`s beneath. The artboard is chevron header with parent label, marker + 27px mono callsign, one-sentence explanation ("Heard in this over. Resolved from the phonetics at 0.94."), mono meta row, waveform card, transcript with the callsign span highlighted, inline "Why this callsign" (lattice slots, prior bars, runner-up), and a bottom action bar `Not right?` / `Confirm`. | design | fixed |
| R-051 | detail › why | `Detail-Why`, FR-UI-8 | The inspection surface prints candidates and priors as text lines ("K7LWH — score 8.60", "  prior: +3.10 (supported)"). The artboard is lattice slots (unit, score, kept alternate; amber border below threshold), a grammar line, a ranked candidate list with the chosen one marked, and prior *bars* — with a prior that argued against filling leftward in amber and a cold-start prior reading `cold start`, no bar. The mapper already distinguishes cold start from negative; the rendering does not. | spec V4 @59383e0: NOT fixed on device — see R-180 / R-181. Close-out: tracked by R-180 (priors/survivors, partial) and R-320 (per-slot lattice, halt); no separate work. | spec | partial |
| R-052 | detail › correct | `Detail-Correct-A/B/C`, `Detail-Propagated`, P3 | Correction is an inline disclosure with a lexicon `TextField` and a free-text `TextField`. The artboard is a sheet with three tiers in order (candidates → lexicon search with match highlighting → typed callsign with the *unverified* warning and a scope choice), and after applying, a `Detail-Propagated` screen: what changed (N overs, voiceprint, priors, 0 deleted), `Undo all`, the affected rows with the old callsign struck through. Today `onCorrect` runs and the detail silently `refresh()`es. | spec | fixed |
| R-053 | detail | `Detail` header, FR-UI-4 | An INFERRED attribution must link to the transmission that confirmed it. `Attribution.inferred(…, sourceId)` carries it; the header renders no link. The artboard sentence is "Matched by voice to **02:14:07**, where the callsign was heard clearly", tappable. | spec | fixed |
| R-054 | detail › playback | `Detail-Playback`, FR-UI-5 | "▶ Play" is a text action; no waveform, no position, no scrub, no speed; "No retained audio" and playback-unavailable are plain sentences. The artboard is the waveform card (§6.16) with playing / no-audio / unavailable variants and the spoken word outlined in the transcript. | design V4 @59383e0: NOT fixed on device — see R-180 / R-181. Close-out: tracked by R-181 (waveform, WP6 round 7 in flight) and R-193 (no-audio copy, fixed); no separate work. | spec | partial |
| R-055 | detail › revisions | `Detail-Revisions`, P9 | Superseded transcripts are a bare list under "Earlier versions (superseded)". The artboard is version cards (current / superseded, pass, model, time, who), a word-level diff against current, and `Restore`. | design | fixed |
| R-056 | detail › label | `Controls` §6.11 | The labelled-sample form uses tap-to-cycle labels ("Outcome: speech", "Certainty: certain") and four unlabelled `TextField`s whose purpose is only in the content description. Closed sets are visible lists; every field has a visible label. | design | fixed |
| R-057 | detail | `Detail-Ambiguous`, `Detail-Unknown` | No state-specific layout. AMBIGUOUS is the chooser (both candidates with their evidence, `Neither`, the lattice slot that split, `Leave ambiguous`). UNKNOWN is "what was tried" (grammar, voice match with nearest, thread context, kept as unidentified) and `I know who this is`. | design | fixed |
| R-058 | detail | `Detail` | The `Confirm` action does not exist (records that a human agreed; feeds calibration; not a fifth state). | spec | fixed |

## Search — WP7

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-060 | search | `Search` | Three different things read "Search": the top bar, the screen title, and the run action — and the action is bare body text indistinguishable from the filter labels above it. Screenshot `07-search.png`. | halt | fixed |
| R-061 | search | `Search-Filters`, `Controls` §6.11 | Band, attribution state and accepted/rejected are tap-to-cycle text labels: the operator cannot see the options, cannot go back (80M → all bands is ~10 taps), and cannot tell they are interactive. The artboard is a filter sheet: chips for frequency/band, checkboxes with counts for state, checkboxes for include, a time control. Screenshot `08-search-filters.png`. | halt | fixed |
| R-062 | search | `Search-Filters`, FR-UI-3 | FR-UI-3 requires a time **range**; only a single `Date (YYYY-MM-DD)` field exists. | spec | fixed |
| R-063 | search | `Search`, `Search-Results`, `Search-Empty`, `Search-Unavailable` | Initial state is four empty text fields; the artboard is a focused query field, recent searches with counts, "Try" hints, Tonight / All nights chips. Results are a flat list; the artboard groups by night, highlights matched words, shows applied filters as dismissable chips and a count line. No-results is "No results"; the artboard names the narrowing filters and offers to widen each with its count. Unavailable is a sentence; the artboard is the amber banner naming what was and was not applied, with `Retry`. | design | fixed |
| R-064 | search | guide §9 | Enum names surface raw: "Attribution state filter: CONFIRMED". Operator strings are prose: `Confirmed`. | design | fixed |
| R-065 | search | `Search` | Search results rows omit time/frequency columns the log row has. Same row component as the Log. | design | fixed |

## Stations and frequencies — WP8

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-070 | stations | `Stations` | List rows are callsign + "N transmissions · last heard …". The artboard: chips (Tonight / All time / Named / Unidentified), a column-header row, per row a state marker, mono callsign, tonight's count with context ("net control", "12 by voice match · 3 heard"), `NEW` / `CORRECTED` badges, a given name beside the callsign, mono last-heard, and a trailing "4 unidentified voices · 36 overs" row. | design | fixed |
| R-071 | station | `Station` | Detail is title + "N transmissions" + two charts + "THIS WEEK VS LAST" caption lines + non-tappable rows. The artboard: a facts table (overs with `Log`, attribution split, frequencies, first/last heard), "When they are around" with a legend line and a one-sentence summary, `Recent overs` with `All 612`, rows that open the over. Rows must be tappable (intent §0). | design | fixed |
| R-072 | station › pattern | `Station-Pattern`, FR-UI-11/12 | Day-of-week is rendered as a second bar chart. The artboard is an hour × day grid with the not-listening hatch per cell, a three-swatch legend, a By hour / Hour × day / Change over time toggle, and a "What this says" list that names the unknown days and hours explicitly. The week-over-week text lines are the "change over time" data and belong under that toggle. | design | fixed |
| R-073 | station › identity | `Station-Identity`, FR-SPK-10, constitution III | Absent: how the station is known (heard / lexicon / voiceprint cluster / name you gave), the never-leaves-the-device statement, `Rename`, `Split`. | spec | fixed |
| R-074 | frequencies | `Frequencies`, `Frequency`, `Frequency-Change` | List rows are frequency + count. The artboard: what it is (repeater · band · mode), tonight's count and station count, a 14-night sparkline with hatched nights, "busier than usual" in amber. Detail lacks the facts table (offset, tone, listened hours, nets), the typical-night chart, `Regulars`. `Frequency-Change` (departure vs usual, with the cause) is absent. | design V5 @f8430b8: NOT fixed on device — FQ02 facts (Listened / Nets rows), subtitle (offset · tone · type), chart axis labels and caption still missing; see R-216. WP8 @c013149: Listened/Nets rows, chart axis+caption+More added; repeater offset/tone stays omitted (no :data column). | spec | closed |
| R-075 | station, frequency | `Charts` | Chart title says "(UTC)" while `ReaderPolling` buckets day-of-week in `ZoneId.systemDefault()`. Artboards and the operator's expectation are local time on both. Resolve to local, and drop the zone from the title. | spec | fixed |

## Setup — WP9

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-080 | first run | `Setup-Welcome` … `Setup-Done`, `Flow-Setup`, P8 | There is no setup sequence. `MainActivity` requests RECORD_AUDIO then POST_NOTIFICATIONS with bare OS prompts, fires the battery intent, starts capture, opens the reader. P8: a guided sequence, each step verifiable before proceeding. Thirteen boards. | spec | fixed |
| R-081 | setup › input | `Setup-Input`, `Setup-Verify`, `Setup-Route-Mismatch`, FR-CAP-2a, F1 | No input selection, no verified-route step, no halt on mismatch. `getRoutedDevice()` is checked in `:capture-android`; nothing surfaces it. | spec | fixed |
| R-082 | setup › level | `Setup-Level`, FR-CAP-3 | No level step. | spec | fixed |
| R-083 | setup › overnight | `Setup-Battery` | The battery-exemption intent fires with no rationale and no statement that the API lies (the artboard says it in the operator's words). | spec | fixed |
| R-084 | setup › radio | `Setup-Rig`, `Setup-Rig-Usb`, `Setup-Rig-Verified` | No rig setup. | spec | fixed |
| R-085 | setup › mic denied | `Setup-Mic-Denied` | Denying the mic twice leaves the app on "Requesting microphone access…" forever with no path to settings. | halt | fixed |

## Settings, improve, digest, sessions — WP10

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-090 | settings | `Settings`, `Settings-*` (10) | The Settings destination opens straight into F-008's `ModelsScreen` — there is no settings root. The board is `Settings.dc.html` (grouped rows: Capture / Records / Privacy / About, each with a one-line status sub-line) with `Settings-Assets` one row down. Absent: FR-CFG, FR-STO (retention with announced deletion), FR-TIER, FR-RIG, FR-EXP, FR-CON (per-category consent, nothing on by default, the never-included list), FR-OBS (bundle preview), About. | spec | fixed |
| R-093 | settings › models | `Settings-Assets`, `Fail-Lexicon`, guide §6.7 | F-008's `ModelsScreen` (real, and its `ModelsController` logic is right — keep it): the presentation is M3 filled `Button`s ("Download", "Side-load from file") side by side, status as `bodyLarge` sentences ("Installed (checksum verified)"), a free-text `lastMessage`, and the unknown-checksum reason printed in full as a paragraph. The board is one row per asset — marker (green verified / hollow not installed), name with an `active` tag, a sub-line of size · checksum prefix · tier — then a single `Install from a file` outlined button and the checksum/record-count promise beneath; a refused import is the `Fail-Lexicon` screen (what was checked, pass/fail per check, what stays active). Copy per guide §9: no "(checksum unknown — not verified against a published value)" in a row label. | design | fixed |
| R-091 | improve | `Improve`, `Improve-Select`, `Improve-Running`, `Improve-Done`, `Flow-Improve`, P12, FR-REP | `PlaceholderScreen`. P12 calls this a headline capability. | spec | fixed |
| R-092 | earlier nights, digest | `Sessions`, `Session`, `Digest`, `Digest-Item`, FR-DIG | `PlaceholderScreen` for Earlier nights; no session detail (span, coverage chart with gaps, gap list with reasons, session facts); no digest. | spec | fixed |

## Failure states — WP11

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-100 | every | `Fail-*` (22), §12 | Of F1–F22, only F5 has any UI (the unclean-end banner, as a sentence) and `CaptureState.failureReason` is appended to the state label as " — reason". Each failure needs its board: the banner (amber, or red for F1/F16), the copy, the recovery action, and the live-bar variant. | spec | fixed |
| R-101 | capture | `Fail-Route`, F1 | Route mismatch mid-session has no halt UI; the constitution says never continue. | halt | fixed |
| R-102 | notification | `Capture-Notification`, FR-SVC-1 | The foreground notification's content (built in `:pipeline`'s `RealCaptureService`) must be verified against N05: title `Capturing · 6:42 · 412 overs`, second line frequencies · tier (amber when degraded), expanded rows, `Open` / `Stop`. One notification, updated in place. V2 pass 3 @8cbb13a: N05 verified with a real capture — title `Capturing · h:mm · N overs`, second line, expanded rows, Open/Stop, one notification updated in place (`empty/N05-notification-shade-pass3.png`). | spec | closed |
| R-103 | every | `Flow-Degrade` | Recovery is not announced (tier back to 3, rig reconnected, input back) — a toast each. | spec | fixed |
| R-104 | capture status | `Fail-Thermal`, `Fail-Rig`, `Capture-Status` | Found by WP0: `:pipeline` has no thermal signal at all (`ShedSignals` carries battery / backlog / storage only) and no rig-connection-state holder (the rig module is unbuilt, R-084). F7 and F9 cannot be simulated, shown, or built until a process-wide holder exists for each — the `ShedStatus` / `AsrAvailability` pattern. WP11 adds `ThermalStatus` (from `PowerManager.getThermalStatus` + measured RTF) and `RigStatus` (connected / stale-since / absent) in `:pipeline`, and `Scenarios.kt` gains `thermal` and `rig-lost`. | spec | fixed |
| R-105 | capture status | `Fail-Storage`, `Settings-Storage`, FR-STO-3 | Found by WP0: between "fine" and the 100 MiB loud stop there is no "getting low" state — FR-STO-3's early warning (the board's "warned at 3 nights left" and "1 night left" stages) has no signal to render. WP11 adds a storage-forecast facet (nights left at the current rate, thresholds) beside the floor. | spec | fixed |
| R-106 | log gap row | `Rows`, `Fail-Call`, FR-RUN-11, F15 | Found by WP0: `CaptureGapEntity.cause` is a closed enum with no call value and no reason text, so "not listening · 38 s · incoming call" is not representable — the nearest is `INTERRUPTION`. The gap row and the session's gap list both carry the reason on the boards. WP11 extends the cause enum (`CALL`, `INPUT_LOST`, `OS_STOPPED`, `ROUTE_LOST`) and the persister sets it from the `CaptureEvent` cause — a `:data` enum change, lead-approved for WP11 as the one exception to its ownership. WP11a extended `CaptureGapCause` (`CALL`, `INPUT_LOST`, `OS_STOPPED`, `ROUTE_LOST`) and the persister sets it; the `gap-call` row renders "not listening · 38 s · incoming call" (V3 pass 2, R-249). V3 pass 3 confirms. | spec | fixed |
| R-107 | sessions | `Sessions`, `Settings-Tier` | Found by WP0: `SessionEntity.deviceTier` is written (`"T1"` in the `field-tier1` scenario) and read nowhere. The `Sessions` board shows a `TIER 1` chip and "can be improved" on such a session; `Improve` groups by it. WP10. | design | fixed |
| R-112 | capture status, level meter, setup | `Level-Meter`, `Capture-Status` (Level row), `Setup-Level`, `Fail-Level`, FR-CAP-3, F3 | Found by WP4: `:pipeline` publishes no level signal at all (peak / RMS dBFS, noise floor, clipping) — `LevelMeterScreen` can only render its honest `not measured` state and S07 cannot verify a level. WP11c adds `LevelStatus` (holder on the `ShedStatus` pattern, fed cheaply from the capture frame path without ever blocking capture) and `level-low` / `level-clip` scenarios. | spec | fixed |
| R-113 | capture status, setup | `Capture-Status` (Input row), `Setup-Verify`, `Setup-Route-Mismatch`, FR-CAP-2a, FR-UI-7 | Found by WP4: the selected input device, whether its route was verified, the native rate and the resampler identity are known inside `RealCaptureService` at open time but never republished to a process-wide holder, so the Input row reads `not measured` and setup S05 cannot show the four checks. WP11c adds `InputStatus` (device descriptor, route verified / mismatch, native rate, resampler id) published at open and on every route change, and an `input-verified` scenario. | spec | fixed |

## Debug simulator and audit tooling — WP0

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-110 | — | plan §E | There is no way to put the app into a named state for validation. Every phase-E scenario needs `adb shell am broadcast -a org.ort.app.debug.SCENARIO --es name <x>` to load synthetic rows and set status flags, in the debug build only. | process | fixed |
| R-111 | — | plan §E | No script boots a named AVD on a given port, installs, fires a scenario, and screenshots every screen to `results/ui-audit/<scenario>/<screen>.png`. | process | fixed |

## Validator findings — phase E

Filed by the emulator validators (plan §E) against the build named in each row; screenshots under `results/ui-audit/<scenario>/`. Owner = the package re-briefed to fix it.

| id | screen | artboard | what is wrong | sev | status |
|---|---|---|---|---|---|
| R-120 | every Setup screen | `Setup-*` §6.10 | V1 @5c8d144: the step counter "N of 7" renders twice — in the header (correct) and again as a text above the segment row, which the board draws unlabeled. `setup/S02-mic.png` … `setup/S12-ready.png`. WP9. | design | closed |
| R-121 | S05 Setup-Verify | `Setup-Verify`, `Setup-Route-Mismatch`, FR-CAP-2a | V1 @5c8d144: when the 30 s signal check times out, `RouteCheckState.TimedOut` is never handled (`SetupActivity.onVerifyStateChanged` branches only on Mismatch/Passed): all four check icons revert to unfilled, `Continue` stays disabled, and the screen has no back chevron — the operator is trapped. `setup/S05-verify-stuck.png`. WP9. V1 pass 4 @a038128: re-confirmed — Try again and Choose another input both offered after the timeout (`setup/S05-verify-timedout-pass4.png`). | halt | closed |
| R-122 | S04 Setup-Input, S06 | `Setup-Input` | V1 @5c8d144: on a one-mic device four rows all read `sdk_gphone64_x86_64`; only one carries the "Not a radio" refusal, the others read "Unknown · N kHz" with no refusal and no per-type icon; S06 then says "chosen X · routed X" with no way to tell them apart. The row must name the device type (built-in mic / telephony / USB / headset) with the guide §7 icon and refuse every non-radio source. `setup/S04-input.png`, `setup/S06-route-mismatch.png`. WP9. | spec | closed |
| R-123 | S01, S04 (all Setup) | `Setup-Welcome`, `Setup-Input`, guide §4, §11.3, FR-A11Y-3 | V1 @5c8d144: at font scale 2.0 the fixed bottom action bar overlaps the content (S01 items 4–5 hidden behind `Begin`; S04 footnote clipped behind `Verify this input`). Content must scroll clear of the bar. `setup/S01-welcome@2x.png`, `setup/S04-input@2x.png`. WP9. V1 pass 4 @a038128: S01 item 4/5 clipped again at 2.0 on first composition → R-341. WP9 d39131a: covered by R-341's scaffold rebuild; V1 pass 5 confirms. | spec | fixed |
| R-124 | S07 Setup-Level | `Setup-Level`, FR-CAP-3, guide §3 | V1 @5c8d144: the meter draws a static wall of full-height green bars whatever `LevelStatus` says (−38 dBFS looks identical to 0 dBFS clipping); no target band, noise-floor line or clip line; Headroom reads "-0 dB"; the clip state dot uses `halt/*` red although a level warning is a degradation (amber). `level-low/S07-level-low.png`, `level-clip/S07-level-clip.png`. WP9. | design | closed |
| R-125 | S11 Setup-Rig-Verified | `Setup-Rig-Verified`, FR-RIG-2/3 | V1 @5c8d144: `RenderRadioVerified` returns early for any `RigStatus` other than `Connected` — a `Stale` rig (the real `rig-lost` case, routed here by `onChooseRadio`) renders a blank black screen with no header, back or copy. `rig-lost/S11-check.png`. WP9. V1 pass 4 @a038128: re-confirmed on `RigStatus.Stale` (`rig-lost/S11-stale-check-pass4.png`). | halt | closed |
| R-126 | F01 Fail-Route | `Fail-Route` | V1 @5c8d144: the takeover has no status-bar inset — "Halted" and its dot collide with the clock; the board's "Why this halts" link is missing; the subtitle reads "as of HH:MM:SS — capture will not continue on this route" instead of "HH:MM:SS · after H:MM:SS · N overs kept". `input-mismatch/F01-fail-route.png`. WP11b. | design | closed |
| R-127 | F03 Fail-Level banner | `Fail-Level`, guide §11 | V1 @5c8d144: the amber banner starts at y=0 with no status-bar inset; icon and title overlap the clock. Every banner/takeover in `FailureHost` needs the system-bar inset. `level-low/F03-fail-level-banner.png`. WP11b. | design | closed |
| R-128 | F16 Fail-Usb + live bar | `Fail-Usb`, guide §6.6 | V1 @5c8d144: while the USB-permission failure shows, the live bar still reads nominal "Live" in green instead of the degraded amber "audio fine · radio needs permission" with the red `Act` label — the debug failure override (and any F-id the mapper raises) must drive the live-bar tone. `usb-permission/F16-fail-usb.png`. WP11b (may edit `LiveBarPolling.kt`). | design | fixed |
| R-130 | CF01, R01, DG03 (WP10 root destinations) | `Settings`, `Improve`, `Sessions` | V6 @5c8d144: two stacked drawer-icon rows — the host header plus a second bare hamburger row from the screen's own scaffold — push content down (regresses R-003/R-004 on the WP10 screens). `overnight/CF01-settings.png`, `field-tier1/R01-improve.png`, `stations-14-nights/DG03-sessions.png`. WP10. | design | closed |
| R-131 | CF01 Settings | `Settings` | V6 @5c8d144: rows lack their 18px leading icons; Tier sub-line drops "· this phone's best · what it does not know". `overnight/CF01-settings.png`. WP10. V6 pass 2 @59e42c4: NOT fixed on device — no leading icons, Tier sub-line unchanged; see R-253. WP10 f3e0d4f: NavRow icons (round 5) + Tier sub-line verbatim. V6 pass 3 @1b3f645: confirmed (`overnight/CF01-settings-pass3.png`). | design | closed |
| R-132 | CF02 Settings-Capture | `Settings-Capture`, intent CF02 | V6 @5c8d144: no row actions at all — no `Change` beside Device, no "Re-verify the route now", no `Meter` beside Speech (→ N06), no `Edit` beside the manual frequency; only the back chevron is clickable. `overnight/CF02-settings-capture.png`. WP10 (+ WP9 `SetupActivity` step extra for re-verify). WP3 r6: Meter → level meter route wired. | spec | closed |
| R-133 | CF03 Settings-Storage | `Settings-Storage`, FR-STO | V6 @5c8d144: retention model differs from the board — no Lexicon category, no colour-coded usage bar + legend, "Keep audio for N nights · Change" replaced by GB-budget chips plus a "prune oldest first" toggle not on the board; the "Warn at 3 nights left" / "stop retaining audio" / "Hard floor" rows are absent. `overnight/CF03-settings-storage.png`. WP10. V6 pass 3 @1b3f645: still missing the 4-segment usage bar with Lexicon, nights-based "Keep audio for N nights · Change", and the "Next deletion" row with `Review`; device has a budget-chip row, prune toggle and amber banner the board lacks (`overnight/CF03-storage-pass3.png`). WP10 dec5c01: the what-will-be-deleted banner now gates on FR-STO-3a's real trigger; GB-budget chips and auto-prune toggle kept because FR-STO-3/D26 and FR-STO-3a/AC-124 mandate them (board's nights control is the spec outlier — design-intent to be corrected in phase G). Still missing: Lexicon usage category and the "Next deletion" row — need per-category byte accounting and a prune-candidate from the shed controller (`:pipeline`, WP11c). WP11c 10cb2d1: `StorageAccounting`/`measureStorageAccounting`, `SessionStorageSummary`, `NextDeletion`/`computeNextDeletion` in `:pipeline` (lexicon bytes honestly 0 — no durable lexicon file exists). WP10 rendering next. WP10 1fc3a7e: four-segment usage bar from `measureStorageAccounting` (Lexicon honestly 0, own swatch) and the board's "Next deletion: <date>" row with session/overs/size + `Review` from `computeNextDeletion` (honest fallback when null). `Review` → DG04 host wire with WP3 round 10. GB budgets kept per FR-STO-3/D26 (design-intent to be corrected). | design | fixed |
| R-134 | CF05 Settings-Tier | `Settings-Tier`, P11 | V6 @5c8d144: the P11 content ("what it therefore does not know" bullets, THE TIERS explanation, override rows' consequence sub-lines) is missing — static copy that needs no live signal; the honest "no tier detector" disclaimer is right but stands alone. `overnight/CF05-settings-tier.png`. WP10. | spec | closed |
| R-135 | CF07 Settings-Export | `Settings-Export`, constitution III | V6 @5c8d144: "1 sessions"; Include-row sub-lines missing; no `Save file`; the never-exported promise ("Never exported, by any option: voiceprints, names you gave stations, notes, your location…") is replaced by an "Export is not available" banner — the promise must show even while export is unbuilt. `overnight/CF07-settings-export.png`. WP10. | spec | closed |
| R-136 | CF08 Settings-Contribute | `Settings-Contribute`, FR-CON | V6 @5c8d144: the never-included list is not verbatim — "Voiceprints and embeddings" for "Voiceprints"; "Station knowledge" title + its sub-line merged and cut to "who is a regular where, and when". Defaults-off and toggles are correct. `overnight/CF08-settings-contribute.png`. WP10. | design | closed |
| R-137 | CF09 Settings-Diagnostics | `Settings-Diagnostics`, FR-OBS | V6 @5c8d144: 1 of 3 summary tiles (no RTF, no failed-passes); a ~1180 px blank gap before the file list; file descriptions drop clauses and sizes; header "WOULD INCLUDE 7 FILES" vs "In the bundle · 7 files · 2.1 MB". `overnight/CF09-settings-diagnostics.png`. WP10. V6 pass 3 @1b3f645: 3 tiles now, gap gone; still missing the "· 2.1 MB" total, each file's trailing clause, per-file sizes, the scrubbing example, and the Preview/Save bar (`overnight/CF09-diagnostics-pass3.png`). WP10 dec5c01: file clauses and the scrubbing example board-verbatim. Still missing: byte total and per-file sizes and the Preview/Save bar — no diagnostics-bundle producer exists (FR-OBS); commissioned as WP11e. WP11e 243bc79: real producer — `DiagnosticsBundleBuilder.preview/write`, closed seven-file `DiagnosticsBundleSpec`, callsign scrubber, `AC_109`/`AC_120` proofs, `includeAudio` gated off; logs are honest placeholders until WP11c's FR-OBS-1 writers land. WP10 wires Preview/Save next. WP10 948fe55: list and "· N files · X MB" total from the real `DiagnosticsBundleBuilder.preview`; `Save bundle` writes the zip via SAF; amber note gone; Preview is an in-app listing of the same entries (accepted — external readers out of scope). Logs now real via WP11c's `DiagnosticsLog`. | design | fixed |
| R-138 | CF10 Settings-About | `Settings-About` | V6 @5c8d144: no Runtime (ONNX) row, no title icon, version shows "0.1.0" without build/commit ("1.0.0 · build 412 · 6aaa608" pattern). `overnight/CF10-settings-about.png`. WP10. V6 pass 3 @1b3f645: icon and Runtime row present; still no commit hash in the version line, no library versions on Models/Runtime/Radio, row order differs (`overnight/CF10-about-pass3.png`). WP10 dec5c01: version line `<version> · build <n> · <git short commit>` via `BuildConfig.GIT_SHORT_COMMIT`; Models row shows the catalog's sherpa-onnx version; board row order. ONNX Runtime ships inside sherpa-onnx and usb-serial-for-android is not a dependency of this build, so no separate versions can be shown honestly — accepted deviation. | design | fixed |
| R-139 | Now, F13 banner | `Fail-Model`, guide §6.7 | V6 @5c8d144: "Install a model" looked tappable but did nothing. WP3 round 3 (3e2d4ee) wired `onOpenModels` → Settings root; V2 re-checks at 3e2d4ee; WP10's `initialScreen` then lands it on Assets. `model-missing/now-f13.png`. WP3/WP10. V2 @3e2d4ee: navigates to Settings root now; Assets landing pending WP10 `initialScreen` + WP3. WP3 round 5: Now's "Install a model" → Settings Assets via initialScreen. V6 pass 3 @1b3f645: confirmed, lands on Settings-Assets (`model-missing/now-f13-pass3.png`). | spec | closed |
| R-140 | CF04 Settings-Assets | `Settings-Assets`, guide §6.11 | V6 @5c8d144: three rows per Whisper variant (encoder/decoder/tokens) instead of one per model (group them, or state the split honestly as one model row with parts); a failed download renders raw exception text ("Unable to resolve host…") instead of the amber `FailedState` with operator wording and a retry. `model-missing/CF04-assets.png`, `model-missing/CF04-download-tapped.png`. WP10. V6 pass 3 @1b3f645: failed download now a proper amber `FailedState` with Retry; body still quotes the raw host-resolution error, and the three-rows-per-Whisper-variant structure remains (`model-missing/CF04-download-failed-pass3.png`). WP10 dec5c01: one grouped row per Whisper model (`GroupedAssetRow`); failed-download body in operator language with the raw cause behind Details. | design | fixed |
| R-141 | R01, R02, R04, DG01, DG03 | `Improve`, `Sessions`, `Digest` | V6 @5c8d144: "1 overs", "1 session(s)", "N gap(s)" — plural forms not handled (shared helper); R01 summary not in the `bg/card` container; group row reads "Captured at T1" instead of "Captured at tier 1 on the field phone". `field-tier1/R01-improve.png`. WP10. V7 @b94bb5e: Now's CAN GET BETTER item still says "captured at T1 tier" — see R-266. | design | closed |
| R-142 | R02 Improve-Select | `Improve-Select` | V6 @5c8d144: the three Pass checkboxes have no sub-lines ("Pass B · was whisper-tiny · expect fewer transcript errors"); the estimate is two sentences instead of the Time/Battery/Storage/Corrections rows (honest "not estimated" is fine, the rows are not). `field-tier1/R02-improve-select.png`. WP10. | design | closed |
| R-143 | R03 Improve-Running, R04 Improve-Done | `Improve-Running`, `Improve-Done`, FR-REP | V6 @5c8d144: R03 unreachable — the fake runner finishes instantly with 1 over (the fake must step per record with a visible cadence, or the `field-tier1` scenario must seed enough overs); R04 honestly claims nothing but shows none of the board's "What changed" breakdown or `Review the N changes` because nothing is recomputed — the reprocessing engine is absent from `:pipeline` (R-091). `field-tier1/R03-improve-running.png`. WP10 (fake cadence) + pipeline follow-up. WP11d (2d11a3b): real `ReprocessRunner` in :pipeline (Pass B at current tier, pausable, capture-safe, `ReprocessStatus.Done(summary)` with measured counts); `ImproveContent` must switch to `RealImproveRunner` (WP10). WP10 aa2c1b5: ImproveContent uses RealImproveRunner; Done shows measured counts. V6 pass 3 @1b3f645: real runner reaches R03 but crashes on missing retained audio → R-290 (halt). | spec | partial |
| R-144 | DG03 Sessions | `Sessions` | V6 @5c8d144: no month group headers across the month boundary; gap captions lack the hatch icon and `accent/gap` amber; plural bug. `stations-14-nights/DG03-sessions.png`. WP10. | design | closed |
| R-145 | DG04 Session | `Session` | V6 @5c8d144: "COVERAGE" label rendered twice; chart caption shows raw "1320s" where the gap list formats "22m 0s"; Overs row lacks `Log`; Models row absent; action bar 2 of 3 buttons (no `Export`), outlined not `bg/chip`. `stations-14-nights/DG04-session.png`. WP10. | design | partial |
| R-146 | DG01 Digest | `Digest`, FR-DIG | V6 @5c8d144: WORTH KNOWING omitted instead of the app's empty-state sentence; BY THE NUMBERS 1 of 3 tiles (no attributed %, no rejected); no `Export this night`; raw "1320s". `stations-14-nights/DG01-digest.png`. WP10. | design | closed |
| R-147 | F14 Fail-Clock, F17 Fail-Interrupted | `Fail-Clock`, `Fail-Interrupted` | V6 @5c8d144: render as a small top card over Now with Now bleeding through; none of the boards' content (facts table, "Around the change" / "The 3 overs" log, drill-in from Earlier nights). Needs a full-screen presentation like F19–F22 and, eventually, a Session-detail embedding. `clock-dst/F14.png`, `interrupted-pass/F17.png`. WP11b. | spec | closed |
| R-148 | F19, F21, F22 | `Fail-Reconcile`, `Fail-Asset-Swap`, `Fail-Calibration` | V6 @5c8d144: titles render behind the status bar (no top inset — F20 is correct); trailing sentences and per-row sub-lines dropped (F19 no `Play` on orphan files; F21 no option sub-lines / lexicon markers; F22 chart has no axis labels, no "Dots below the diagonal are over-confident" caption, no closing paragraph). `reconcile/F19.png`, `asset-swap/F21.png`, `calibration/F22.png`. WP11b. | design | closed |
| R-149 | F6 storage-warn banner, F8 backlog banner | `Fail-Storage`, `Fail-Backlog` | V6 @5c8d144: banners and live-bar tones correct, but F8's live bar says "Tier 0" where the board shows queue depth ("41 behind"); F6 lacks "How this unfolded"; F8 lacks the 30-minute queue chart and Waiting/Rate/Capture/In-the-log rows. `storage-warn/F6-banner.png`, `backlog/F8-banner.png`. WP11b (+WP4 live-bar label). V6 pass 2: F6 confirmed fixed; F8 Rate row + queue chart still absent — see R-254. WP11b e4d0e8d: F8 Rate row + queue chart added. | design | fixed |
| R-150 | CF03 Settings-Storage @2x | `Settings-Storage`, guide §4, AC-63 | V6 @5c8d144: at font scale 2.0 the "Unlimited" chip wraps one character per line down the screen and the legend wraps "Rec/ords/0.0/GB" — chip and legend rows must reflow or scroll, never wrap intra-word. `overnight/CF03@2x.png`. WP10. V6 pass 2: still wrong — rows now clip instead of wrapping; see R-251. Close-out: tracked by R-251 (legend, partial) and R-291 (chip row, fixed by WP10 dec5c01); no separate work. | spec | partial |
| R-151 | R02 @2x, F20 @2x | `Improve-Select`, `Fail-Migration`, guide §5 | V6 @5c8d144: the fixed bottom action bar clips the scrolled content at 2.0 (R02 button text unreadable; F20 last bullet cut mid-word behind `Rebuild now`). Same class as R-123 — the shared pattern (content scrolls above a bar with bottom padding = bar height) should live in one place. `field-tier1/R02@2x.png`, `migration-failed/F20@2x.png`. WP10 + WP11b. | spec | fixed |
| R-152 | DG04 @2x, N04 @2x | `Session`, `Capture-Status` | V6 @5c8d144: fixed-width label columns do not grow with scaled text — label and value collide ("Stations5"). `KeyValueRow` should size the label column by content. `stations-14-nights/DG04@2x.png`. WP2 (`KeyValueRow`). | polish | fixed |
| R-153 | F18 Fail-Pass | `Fail-Pass`, FR-RUN-9 | V6 @5c8d144: absent from the failure model entirely — no `FailurePresentation` case, no scenario, not even listed among the no-signal ids. The board is a detail state (a pass that errored repeatedly: failed, visible, manually retryable). WP6 (detail state) + WP0-style scenario `pass-failed` (WP11b owns `Scenarios.kt`). | spec | closed |
| R-154 | F12 Fail-Lexicon | `Fail-Lexicon`, FR-LEX-30, FR-AST-2 (the register's earlier "FR-LEX-12" cite was a naming clash with the spec's actual, different FR-LEX-12 — see `lexicon` package's CHANGELOG entry) | V6 @5c8d144: was unbuilt — no lexicon-import validator existed anywhere in the codebase. Now built: `:lexicon`'s `org.ort.lexicon.import` package (`LexiconImportValidator`, `LexiconImportInstaller`) runs the board's checks (manifest, checksum, record count/shape, callsign grammar sample, no duplicate keys) and keeps the previous lexicon active on any failure; `ModelsController.installLexicon` (`:app`) is the real call site, exposing `LexiconImportViewState` for a screen to render; the `lexicon-corrupt` debug scenario (`Scenarios.kt`) runs this real validator against a genuinely corrupt bundled asset and produces a real `Rejected` result, reachable at `LexiconCorruptScenario.lastResult`. **Still open:** no screen renders it — `Fail-Lexicon.dc.html` has no WP10 build, so the refusal is not yet visible on the emulator (`ModelsScreen.kt` untouched, per this package's file ownership). WP10 owns wiring `LexiconImportViewState` into a real screen. V6 pass 3 @1b3f645: confirmed full refusal screen (`lexicon-corrupt/F12-fail-lexicon-pass3.png`). | spec | closed |
| R-129 | Log (every route) | `Log`, `Rows`, `Log-Filter`, `Log-Partial`, `Log-Empty`, `Log-Rejected` | V3 @3e2d4ee: opening Log crashes the process — `LogContent.kt:50` keeps `LogQuickFilterId` (a plain `sealed interface`) in `rememberSaveable` with no `Saver` → `IllegalArgumentException: MutableState containing All cannot be saved…` on first composition; 100 % reproducible; blocks L01–L05 entirely. Robolectric did not catch it because no `SaveableStateRegistry` is installed under `createComposeRule` — add an `ActivityScenario`-hosted smoke test for every destination. `overnight/L01-log.png`. WP5. | halt | closed |
| R-160 | T01 Threads | `Threads` | V3 @3e2d4ee: thread cards omit the kind token — "145.230 · 4 OVERS" instead of "QSO · 145.230 · 4 overs" — visually and in the content description (kind is honestly null in the data per WP5; the board's line must then read the frequency-first form the guide prescribes for unknown kind, or the kind must be derived when the data allows: ≥2 stations alternating = QSO). `overnight/T01-threads.png`. WP5. | design | closed |
| R-161 | T02 Thread-Detail | `Thread-Detail` | V3 @3e2d4ee: header meta drops kind and mode ("145.230" for "QSO · 145.230 · FM"), the span line drops the duration ("· 1 m 55 s"), and the per-over table has an empty FREQ column the board does not have (TIME / OVER only). `overnight/T02-thread.png`. WP5. | design | closed |
| R-162 | T02 "How these were attributed" | `Thread-Detail`, `States`, guide §6.1, §11.2 | V3 @3e2d4ee: the CONFIRMED line's content description reads "filled circle, ✓ CONFIRMED, confidence 0.85" — a confidence on a CONFIRMED marker, which the design forbids; visually correct. The legacy `AttributionMarker` description must not name a confidence when the chip is not shown (and the "✓" glyph must go). WP5 (use `AttributionRow`) + WP2 (shim description). | spec | closed |
| R-163 | T03 Threads-Ungrouped | `Threads-Ungrouped` | V3 @3e2d4ee (`corrected`): "1 overs · not yet grouped", "1 overs · 0 stations heard" (plurals); the paragraph says "at the tier it is currently running" instead of naming the tier while the link below says "tier 1" — read the shed tier the same way; the ambiguous marker sits on its own line above the headline instead of inline; "By frequency, meanwhile" is not in `sectionLabel` style. `corrected/T03-threads-ungrouped.png`. WP5. | design | closed |
| R-164 | every destination while a banner shows | `Main` header, guide §11 | V3 @3e2d4ee (`backlog`): the three-line amber banner is a fixed top overlay with no status-bar inset and covers the whole header — drawer icon, live dot, search — so the drawer and search are unreachable while it shows. Same root as R-127; banners must sit below the header (inside the content column) or the header must stay above them. `backlog/T01-threads-live-header.png`. WP11b (+ WP3 for a header slot if needed). | halt | closed |
| R-170 | Now-Idle earlier nights, Sessions | `Now-Idle`, `Sessions` | V2 @3e2d4ee: `ReaderPolling.nightDateFormat` uses `Locale.ROOT` with `MMM`, rendering "Tue 8 M09" instead of "Tue 8 Sep" on every night row. Use the device locale (guide §9 dates). `overnight/N01-now.png`, `empty/N02-idle.png`. WP4. | design | closed |
| R-171 | N01, N04 live | `Main`, `Capture-Status` | V2 @3e2d4ee: no scenario marks capture running on a session carrying `overnight`'s rich data, so the populated **live** Now and Capture-Status boards are unreachable on the emulator (`nowViewState`/`captureStatus` honestly fall back when the ids mismatch). Add `overnight-live`: the overnight fixture with `endedAt = null`, `CaptureState` capturing on that session id, heartbeat fresh. `overnight/N01-now.png`. WP4 (may edit `app/src/debug/**`). V2 pass 3 @8cbb13a: live N01/N04 confirmed on `overnight-live` (`overnight-live/N01-now-pass3-retry.png`, `N04-capture-status-pass3.png`). | spec | closed |
| R-172 | N04 Capture-Status | `Capture-Status` | V2 @3e2d4ee: after starting a real capture from Now, Capture Status mixes live facts (input, level, thermal, tier) with counts from the unrelated fixture session ("42 captured · 1 rejected", "Since 08:49 · 6:56:22" for a minute-old session) — `CaptureStatusContent`'s `sessionId` is fixed at reader launch and never follows the id `ReaderPolling.startCapture` returns. The status must always read `CaptureState.sessionId` while capturing. `overnight/N04-real-running.png`. WP4 (+ WP3 if the host must re-key). V2 pass 3 @8cbb13a: re-confirmed with a real capture start/stop (`empty/N04-real-capturing-pass3.png`). | halt | closed |
| R-173 | Now-Idle, Sessions, every real session | `Now-Idle`, `Sessions`, FR-RUN-12 | V2 @3e2d4ee: `RealCaptureService` never writes `SessionEntity.endedAt` on stop (`stopCaptureInternal` only clears `CaptureState` and the heartbeat), so every real session — even one stopped through the confirmed dialog — reads "HH:MM – still running" forever and survives the `empty` scenario. `unclean-end/F05-fail-killed.png`, `empty/N02-idle.png`. WP11c (`:pipeline`). V2 pass 3 @8cbb13a: re-confirmed — stopped session shows a real end time (`empty/N02-idle-after-stop-pass3.png`). | halt | closed |
| R-174 | N03 Now-First (any fresh session) | `Now-First` | V2 @3e2d4ee: a 0-over session renders `Main`'s full-width hatched "not listening" chart instead of `Now-First`'s flat baseline with "the chart fills as the night goes on", and the subtitle drops "listening on 145.230 and 146.960". `NowViewState` needs the first-session case (no overs yet, capturing). `first-session/N03-now-first.png`. WP4. | design | closed |
| R-175 | N06 Level-Meter | `Level-Meter`, guide §6.6 | V2 @3e2d4ee: missing the "Weakest over resolved tonight" row, the right-edge dB axis labels (0 / −12 / −18 / −58), the bottom state sentence with dot ("In the band, nothing to adjust."), and the pinned live bar. `overnight/N06-level-meter-real.png`. WP4. | spec | closed |
| R-176 | Now, Stations heard | `Main` | V2 @3e2d4ee: "N unidentified overs" (and "1 unidentified overs") where the board says "4 unidentified voices" — wrong noun and no plural handling. `model-missing/now-f13.png`. WP4. | design | closed |
| R-177 | Fail-Thermal banner | `Fail-Thermal` | V2 @3e2d4ee: "dropped to tier N at HH:MM:SS" advances on every poll (08:59:05 → :26 → :48 on an untouched scenario) — the time must be the real transition moment. `ThermalStatus.Hot` needs a `sinceMillis` (like `RigStatus.Stale`) set at the transition, and the mapper must use it. `thermal/F07-banner.png`. WP11c (holder) + WP11b (mapper). | spec | fixed |
| R-178 | every banner @2x | `Fail-Storage`, `Main`, `Capture-Status` | V2 @3e2d4ee: at font scale 2.0 the banner grows but the content beneath does not move down — Now's failed-state title and Capture-Status's elapsed line render behind the banner's bottom edge. Resolves with the banner-in-content-column fix (R-164). `storage-warn/F06-banner@2x.png`, `storage-warn/N04-capture-status@2x.png`. WP11b. | spec | fixed |
| R-179 | recovery toasts (R-103) | `Flow-Degrade` | V2 @3e2d4ee: could not be exercised — `scenario.ps1` force-stops the app before each broadcast, so no running UI observes a degraded→nominal transition; a raw broadcast without force-stop did not visibly change the thermal state within ~6 s. Needs a `-NoRestart` option on `scenario.ps1` (or a documented raw-broadcast recipe) and a check that the receiver updates holders in-process. WP4 (`tools/ui-audit`, `app/src/debug`). V2 pass 3 @8cbb13a: thermal and input recoveries confirmed with `-NoRestart` (`thermal/failure-toast-tier-pass3.png`, `input-mismatch/failure-toast-input-pass3.png`). | spec | closed |
| R-200 | Search initial | `Search` | V5 @f8430b8: the query field is not focused on entry (no keyboard); the screen shows the standard drawer+search header instead of the board's back-chevron + inline query field. `search-corpus/Q01-search.png`. WP7 (+WP3 for the header variant when SEARCH is current). WP7: focus + back-chevron header done; WP3 must suppress the host header for SEARCH. WP3 round 5: host header hidden on SEARCH; back chevron "Back". | spec | fixed |
| R-201 | Search TRY hints | `Search` | V5 @f8430b8: "Words from a transcript — park activation" renders the example in mono; the board keeps prose examples in sans, mono only for callsign / frequency / POTA. `search-corpus/Q01b-typing.png`. WP7. | design | closed |
| R-202 | Search-Filters | `Search-Filters` | V5 @f8430b8: every attribution / include count reads 0 and the CTA reads "Show 0 overs" at default filters on a non-empty corpus — the empty state's own widen suggestion computes 14 for the same filters. `search-corpus/Q02-filters.png`. WP7. | halt | closed |
| R-203 | Search-Filters | `Search-Filters` | V5 @f8430b8: frequency/band chips are a generic HF list (160M…20M) instead of the heard frequencies (145.230 / 146.960) plus their bands (2 m / 70 cm). `search-corpus/Q02-filters.png`. WP7. | design | closed |
| R-204 | Search results | `Search-Results`, `Search-Unavailable`, FR-UI-3 | V5 @f8430b8: every text query produces "Text search is unavailable right now" (100 % reproducible from a fresh `search-corpus`; `Retry` does not help) — the real grouped/highlighted results are never reachable. Capture the actual exception on device (logcat) — if the emulator's SQLite lacks FTS5, `:data` needs a bundled SQLite or an FTS4/LIKE fallback (lead decision once diagnosed). `search-corpus/Q05-unavailable.png`. WP7 (diagnose) → `:data`. DIAGNOSED (WP7): the API 34 emulator's platform SQLite has no fts5 module ("no such module: fts5" at CREATE VIRTUAL TABLE); `OrtDatabase` assumes fts5 on minSdk 26 and only builds the index in onCreate. Lead decision: bundle a SQLite with FTS5 in `:data` (also fixes Robolectric's missing fts5). :data bc9bc51/c009d7e: bundled `androidx.sqlite:sqlite-bundled` with FTS5 behind `BundledSQLiteDriver` (Room callbacks now run via `inWriteTransaction`/`execRaw`), 10 s busy timeout, schema v4. Text search works on API 34 and under Robolectric. V5 search half confirms on device. | halt | fixed |
| R-205 | LogRow time/frequency columns | `Rows`, guide §6.5 | V5 @f8430b8: the shared row's mono time (52px) and frequency (56px) columns wrap mid-value at default scale ("16:28:5" / "6") on search results and station recent overs; 3 lines at 2.0. Columns must fit their content (`widthIn(min)` + no wrap, `maxLines = 1`, and the guide's column widths). `search-corpus/Q03-back-intact.png`, `stations-14-nights/ST02-detail.png`. WP2. | design | closed |
| R-206 | Stations list | `Stations`, `States` | V5 @f8430b8: every row shows the Unknown marker and label regardless of the station's real attribution (WA7HJR has 10/10 CONFIRMED on its own detail; the row's description even names the callsign). The dominant-state derivation or the row's marker binding is wrong. `stations-14-nights/ST01.png`. WP8. | halt | closed |
| R-207 | Stations list | `Stations` | V5 @f8430b8: column headers read TIME / FREQ / STATION / LAST (a Log header) instead of station / tonight / last; no "N heard all time · M tonight" subtitle; no `Most heard` sort action; no trailing "N unidentified voices · M overs" row; last-heard is a raw "2026-09-08 05:37 UTC" instead of local mono time. `stations-14-nights/ST01.png`. WP8. | design | closed |
| R-208 | Station detail | `Station` | V5 @f8430b8: no state marker beside the callsign title; the context sentence ("Net control, Tuesday night net · heard 14 nights of 14") is missing; First / Last heard are raw ISO timestamps with a literal UTC suffix (R-075 fixed the chart title only). `stations-14-nights/ST02-detail.png`. WP8. | spec | closed |
| R-209 | Station-Pattern, Hour × day | `Station-Pattern` | V5 @f8430b8: the grid is transposed — hours as rows and days as columns with no hour labels; the board is days as rows (Mon…Sun on the left) and hours as columns labelled along the top; legend reads "heard / quiet" instead of "listened, not heard / heard often". `stations-14-nights/ST03-hourxday2.png`. WP8 (+WP2 `DayOfWeekGrid` orientation). | design | closed |
| R-210 | Station-Pattern | `Station-Pattern` | V5 @f8430b8: subtitle reads "Local time" instead of "14 nights of listening, 25 Aug – 7 Sep". `stations-14-nights/ST03-pattern2.png`. WP8. | polish | closed |
| R-211 | Station-Pattern @2x | `Station-Pattern`, guide §6.3 | V5 @f8430b8: at font scale 2.0 the third mode chip ("Change over time") is clipped off-screen and the chip row does not scroll — chip rows must scroll horizontally when they overflow. `stations-14-nights/ST03@2x.png`. WP2 (`FilterChipRow` scrolls) + WP8. | spec | closed |
| R-212 | Station-Identity, Frequency regulars | `Station-Identity`, `Frequency` | V5 @f8430b8: "heard 10 time(s)", "6 over(s) · 6 session(s)" — the `(s)` placeholder leaks into copy; use the shared plural helper. `stations-14-nights/ST-kebab2.png`, `FQ02-detail.png`. WP8. | design | closed |
| R-213 | Station-Identity, Voice | `Station-Identity` | V5 @f8430b8: "One cluster, 0 overs" while the sub-line says "10 with the callsign heard · 0 inferred" — the total must equal the breakdown. `stations-14-nights/ST-kebab2.png`. WP8. | spec | closed |
| R-214 | Station-Identity | `Station-Identity` | V5 @f8430b8: no state markers on the Callsign / Lexicon / Voiceprint / Nearest rows and the HEARD section label is missing. `stations-14-nights/ST-kebab2.png`. WP8. | design | closed |
| R-215 | Frequencies list | `Frequencies` | V5 @f8430b8: no "5 heard all time · 2 tonight" subtitle under the title. `stations-14-nights/FQ01.png`. WP8. | polish | closed |
| R-216 | Frequency detail, Frequency-Change | `Frequency`, `Frequency-Change` | V5 @f8430b8: facts table lacks Listened (nights of N · hours) and Nets rows; subtitle shows "2 m · FM" without repeater offset / tone / type; the hour chart lacks axis labels, the "no hatch: always listening here" caption and `More`. FQ03 is unreachable because no scenario seeds a busier-than-usual night — add `frequency-change` to `app/src/debug`. `stations-14-nights/FQ02-detail.png`. WP8. V5 pass 3 @0455401: re-confirmed on `frequency-change` (`frequency-change/FQ02-146960-pass3.png`). | spec | closed |
| R-180 | D01–D03 Why section, D05 | `Detail-Why`, FR-UI-8 | V4 @59383e0: "Why this callsign" is a raw text dump ("Lattice: ACOUSTIC · model phonetic-lattice-v1", prior rows labelled `callsign-history` / `database` — key names, not guide §9 prose) with no lattice slots, prior bars or ranked candidates; **`Full lattice` is a dead tap** — the same screen re-renders, D05 is never reached. R-051 reopened. `overnight/D05-why.png`. WP6. WP6 a9c24c6: real why-section components; "Full lattice" route confirmed working (stale `screens.json` coordinates). V4 pass 2 @ee4fd07: `Full lattice` opens D05 with real priors and survivors, but section 1 still has no per-slot lattice → R-320 (halt). | halt | partial |
| R-181 | D01–D04, F18 | `Detail`, guide §6.16 | V4 @59383e0: `WaveformCard` draws only the play button and duration — the bar row is empty on every over, idle or playing. R-054 reopened. `overnight/D01-confirmed-rich.png`. WP6 (+WP2 if the card needs real samples). WP6 a9c24c6: not done — needs decoded audio; revisit once WP4 seeds real FLAC (R-290). WP6 c6b1e2c: `WaveformSummaryComputer` (96 peak buckets from the decoded retained audio via the same `FlacSegmentAudioProvider` path `play()` uses, cached per transmission; fake ships) feeds `WaveformCard`; no audio → `NoAudioNotice`, never fake bars; tests `R_181`. | design | fixed |
| R-182 | D01, D03 transcript | `Detail-Confirmed`, FR-UI-4 | V4 @59383e0: the callsign span highlights only a literal callsign string; a phonetically spelled callsign ("whiskey seven november papa charlie" — the board's own example) gets no highlight. Use the resolver's recorded span, not string search. `overnight/D01-confirmed-rich.png`. WP6. WP6 a9c24c6: not done — needs resolver span data the schema lacks; `:data`/`:lexicon` question. WP6: needs a per-candidate character span (start/end into the transcript) on `CallsignCandidateEntity` — absent from the schema; `:data` + resolver work, parked for phase G decision. lexicon 250dfbb: char spans via `TextDerivedLatticeBuilder.buildAnchored`; `:pipeline` Pass B to call it (sent), :data persists, WP6 highlights. | spec | partial |
| R-183 | D02 Inferred header | `Detail`, FR-UI-4, R-053 | V4 @59383e0: "Not heard in this over. Matched by voice. Confidence 0.70." — no "to 02:14:07, where the callsign was heard clearly" clause and no source-over link when the attribution has a source. `overnight/D02-inferred.png`. WP6. WP6 a9c24c6: real source-over time clause. V4 pass 2 @ee4fd07: confirmed (`overnight/D02-inferred-pass2.png`, `D02-source-over-pass2.png`). | spec | closed |
| R-184 | D08 Correct-A | `Detail-Correct-A`, FR-UI-6 | V4 @59383e0: "THE RESOLVER'S OTHER CANDIDATES" renders with zero rows on every over — Tier A never offers the resolver's runner-ups, so every correction falls to Tier B/C. `overnight/D08-correct-a.png`. WP6. WP6 a9c24c6: derivation verified by test; the zero rows on device are the single-candidate fixture — WP4 to seed a multi-candidate Tier A over (`app/src/debug`). WP4 95267d4: the AMBIGUOUS over in `overnight` and `stations-14-nights` carries three ranked candidates (KE7QRS/KE7QRF/KE7QRZ); `AmbiguousCandidatesFixtureTest`. | halt | fixed |
| R-185 | D09 Correct-B | `Detail-Correct-B`, FR-SPK-7 | V4 @59383e0: "A station heard before" is the same grammar/prefix validator as Tier C — a station heard 4× in the session returns only "K7LWH / United States (K)" with no heard-count, no "voice on file", no "4 of 112". Tier B must search the stations this phone has heard and show the board's evidence. `overnight/D09-correct-b-full.png`. WP6. WP6 a9c24c6: Tier B searches real heard stations, not the lexicon (reverses audit F-018; disclosed in CHANGELOG). V4 pass 2 @ee4fd07: confirmed incl. live filtering (`overnight/D09-correct-b-filtered-pass2.png`). | halt | closed |
| R-186 | D03 Ambiguous header | `Detail-Ambiguous` | V4 @59383e0: the header reads only "or KE7QRF" — the primary candidate's callsign is missing ("KE7QRS or KE7QRF"). `overnight/D03-ambiguous.png`. WP6 (+WP2 `TitleAttributionRow` alternate slot). WP6 a9c24c6: ambiguous header names the primary candidate. V4 pass 2 @ee4fd07: confirmed (`overnight/D03-ambiguous-pass2.png`). | design | closed |
| R-187 | D03 candidates | `Detail-Ambiguous`, FR-UI-4 | V4 @59383e0: both candidates read "a known station" with no evidence or score (board: "heard on this repeater 3 times · Oregon" / "never heard before · Oregon", chips 0.51 / 0.46), each a label plus a separate "Choose X" link instead of one evidence-bearing tappable row. `overnight/D03-ambiguous.png`. WP6. WP6 a9c24c6: chooser rows are one evidence-bearing clickable row with ITU-country evidence and score. V4 pass 2 @ee4fd07: confirmed. | spec | closed |
| R-188 | D04 Unknown | `Detail-Unknown`, P1, P11 | V4 @59383e0: "What was tried" shows 2 of 4 checks (no voice-match count, no thread context) and the transcript caption ("Transcript confidence 0.61 · weak signal, cut off") is missing. `overnight/D04-unknown.png`. WP6. WP6 a9c24c6: not reached (transcript-confidence caption); voice-match/thread-context gaps pre-existing. WP6 79e4afd: transcript confidence caption from `TranscriptEntity.confidence`; voice-match distance and thread context need fields named in the CHANGELOG (parked). | spec | fixed |
| R-189 | D01 after Undo all | `Detail-Propagated`, constitution I | V4 @59383e0: after `Undo all` the over's detail renders as UNKNOWN although the data reverted to CONFIRMED (the search list says "filled circle, Confirmed, W7NPC"); reproduced on a fresh navigation. `overnight/D01-after-undo-fresh.png`. WP6. V7 @b94bb5e: also reproduces ~2 s after applying a fresh typed correction (no Undo) — the poll refresh reverts the detail to UNKNOWN. WP6 a9c24c6: `CorrectionPolling.currentAttribution` keeps a corrected row's INFERRED state with null confidence instead of dropping to UNKNOWN (fixes Undo and fresh-correction paths). V4 pass 2 @ee4fd07: NOT fixed — the patch hardcodes INFERRED regardless of the real prior state → R-321 (halt). | halt | open |
| R-190 | D11 Propagated | `Detail-Propagated` | V4 @59383e0: "1 overs re-attributed", "THE 1 OVERS…"; "0 voiceprint now belongs to VE7ABC" / "0 priors updated" phrase a zero as if it happened — follow the board's zero-row pattern and the shared plural helper. `overnight/D11-propagated2.png`. WP6. WP6 a9c24c6: pluralisation, honest zero-voiceprint wording, `bg/card`, marker+badge. V4 pass 2 @ee4fd07: confirmed (`overnight/D11-propagated-pass2.png`). | design | closed |
| R-191 | D11 Propagated | `Detail-Propagated` | V4 @59383e0: no `bg/card` around WHAT CHANGED; the affected row has no marker or `CORRECTED` badge; the subtitle lacks the method/time clause. `overnight/D11-propagated2.png`. WP6. WP6 a9c24c6: real method/time subtitle. V4 pass 2 @ee4fd07: confirmed. | design | closed |
| R-192 | Station detail → Identity | `Station`, `Station-Identity` | V4 @59383e0: no Identity section on Station detail, F10 judged unreachable. V5 reached ST04 through the header kebab and verified rename — Identity exists as its own screen. WP8: if `Station.dc.html` shows an explicit Identity action/preview add it; make the kebab discoverable (description "Station identity"). `overnight/station-wa7hjr.png`. WP8. WP8: board has no explicit Identity action/row (checked directly); kebab now carries `contentDescription = "Station identity"` and `testTag("station-identity-open")`. V5 pass 3 @0455401: confirmed (`stations-14-nights/ST02-kebab-pass3.png`). | design | closed |
| R-193 | D06 no audio | `Detail-Playback`, FR-UI-5 | V4 @59383e0 (`no-audio`): one sentence, no hollow disc, no title, no retention explanation ("Audio deleted by retention on 8 Aug" / "Transcript, attribution and lattice were kept."). Distinguish never-captured from deleted-by-retention. `no-audio/D06.png`. WP6. WP6 79e4afd: `NoAudioNotice` distinguishes never-retained from deleted-by-retention (inferred from state/inspection; no stored deletion date, so the board's date clause is omitted). | design | fixed |
| R-194 | D07 Revisions | `Detail-Revisions`, P9 | V4 @59383e0: version cards lack the attribution row (marker + callsign + corrected / `or <alt>`) and the closing paragraph ("Restoring an earlier version makes it current… Nothing here can be deleted from this screen"). `revisions/D07-retry.png`. WP6. WP6 79e4afd: version cards carry the real marker/callsign (+CORRECTED), closing note board-verbatim; tests drive the `revisions` scenario shape. | design | fixed |
| R-195 | F18 Fail-Pass | `Fail-Pass`, FR-RUN-9 | V4 @59383e0 (`pass-failed`): "What went wrong" is one line without attempt timestamps or the "Retrying by hand…" guidance; the "Live partial, Pass A" label and caption are absent. `pass-failed/F18.png`. WP6. WP6 79e4afd: "Live partial, Pass A" label/caption and the retry-guidance sentence ("with Pass C paused" omitted — no real signal). | design | fixed |
| R-196 | F18 Fail-Pass | `Fail-Pass` | V4 @59383e0: the failed-pass detail also renders D04's "WHAT WAS TRIED" block — the shared path defaults an unresolved over to the UNKNOWN branch regardless of state (same root as R-189). `pass-failed/F18.png`. WP6. WP6 a9c24c6: failed-pass detail no longer renders the Unknown "what was tried" block. V4 pass 2 @ee4fd07: confirmed (`pass-failed/F18-fail-pass-pass2.png`). | halt | closed |
| R-220 | S02b, S05 (timed out), S06 | `Setup-Mic-Denied`, `Setup-Verify`, `Setup-Route-Mismatch` | V1 pass 2 @7293009: a ghost, semi-transparent copy of the bottom bar's secondary text action ("Check again" / "Choose another input" / "Try this one again") renders again just below the status bar, centred, on every Setup screen with a two-button or disabled-single-button bar — static, byte-identical on repeat. `setup/S02b-mic-denied.png`, `setup/S06-route-mismatch-pass2.png`. WP9. V1 pass 3 @a272464: still renders — see R-280. V1 pass 4: see R-340. | design | open |
| R-221 | S04 Setup-Input | `Setup-Input`, guide §7 | V1 pass 2: the "Unrecognised device type N" rows have no leading type icon, breaking the icon column alignment (a generic device icon belongs there). `setup/S04-input-pass2.png`. WP9. | design | closed |
| R-222 | S06 Setup-Route-Mismatch | `Setup-Route-Mismatch` | V1 pass 2: after choosing the Telephony row on S04, S06 reads "chosen sdk_gphone64_x86_64 (Unknown) · routed … (Built-in microphone)" — the chosen device's type is lost between S04 and S06. `setup/S06-route-mismatch-pass2.png`. WP9. | design | closed |
| R-223 | S02b, S06 | `Setup-Mic-Denied`, `Setup-Route-Mismatch` | V1 pass 2: neither screen renders the header back chevron the boards draw (every other step does). If intentional for halt states, the intent file must say so; otherwise render it. `setup/S02b-mic-denied.png`. WP9. | design | closed |
| R-224 | S05 Setup-Verify | `Setup-Verify`, FR-CAP-2a, R-113 | V1 pass 2 (`input-verified` loaded): the third check still runs its own 30 s probe and times out, and the fourth never shows the resampler identity — S05 ignores the published `InputStatus.Opened` (routeVerified, resamplerId). When `InputStatus` reports the chosen device open and verified, checks 3–4 must read it. `input-verified/S05-resampler-check.png`. WP9. V1 pass 3: fix confirmed by code inspection; not black-box testable on the AVD (no selectable USB route). | spec | closed |
| R-225 | S07 Setup-Level @2x | `Setup-Level`, AC-63 | V1 pass 2: the footer row ("noise −72 · target −18 to −12 · clip 0") collapses into one concatenated run at font scale 2.0 — it must wrap or stack. `setup/S07-level-pass2@2x.png`. WP9. | spec | closed |
| R-226 | S12 Setup-Done @2x | `Setup-Done`, AC-63 | V1 pass 2: the fixed-width label column truncates ("Inout", "Overni") and overlaps the state dot at 2.0 — use WP2's `KeyValueRow` (R-152 fixed its label sizing) instead of a hand-rolled column. `setup/S12-ready-pass2@2x.png`. WP9. | spec | closed |
| R-227 | S07, S12 reachability | plan §E | V1 pass 2: no sanctioned path reaches S07/S12 on an emulator with a silent mic (`stepFor` needs a real `RouteCheckState.Passed`; `EXTRA_STEP` refuses to skip a gate) — the validator edited `SharedPreferences` via `run-as`. Add a debug scenario `setup-verified` that seeds `SetupStore` (input verified, level in band) so S07/S12 open through the real flow; document it. WP9 (+`app/src/debug`). | process | closed |
| R-230 | recovery toasts | `Flow-Degrade`, R-103 | V2 pass 2 @877583c: `rig-lost` → `empty` never fires "Radio reconnected" because `empty` resets `RigStatus` to `Absent`, and `RecoveryAnnouncer` needs `Connected`; `rig-lost` → `first-session` does fire it (mechanism sound). Add a `rig-reconnected` scenario (or make the README recipe use `first-session`) and document. `rig-lost/toast-rig-recovery-pass2.png`. WP4 (`Scenarios.kt`, README). V2 pass 3 @8cbb13a: confirmed via `rig-reconnected` (`rig-lost/failure-toast-rig-pass3.png`). | spec | closed |
| R-231 | recovery toasts | `Flow-Degrade`, R-103 | V2 pass 2 @877583c: "Storage back above the floor" is unreachable — no scenario ever sets `StorageForecast.Fine` (reset gives `NotYetMeasured`). Add a `storage-fine` scenario for the transition after `storage-warn`. WP4 (`Scenarios.kt`, README). V2 pass 3 @8cbb13a: confirmed via `storage-fine` (`storage-warn/failure-toast-storage-pass3.png`). | spec | closed |
| R-232 | Capture-Status with a banner | `Capture-Status`, FR-UI-7, guide §11 | V2 pass 2 @877583c: a failure banner (F03) overlays the title, the "Since … heartbeat" line and `Stop` at font scale 1.0 — the operator cannot read whether capture runs. Same root as R-164/R-178 (WP11b's R-178 fix at b4bac15 post-dates this build: re-check on the next pass; if it persists, `CaptureStatusContent` needs the host's `contentTopPadding`). `overnight/N04-stop-tap-test.png`. WP11b/WP4. WP4: covered generically by the host contentTopPadding (b4bac15); re-check on device. V6 pass 3 @1b3f645: confirmed on `level-low` (`level-low/N04-capture-status-pass3.png`); V2 pass 3 re-checks with `storage-warn`. V2 pass 3 @8cbb13a: confirmed at 1.0 (`storage-warn/capture-status-banner-pass3.png`); at 2.0 the banner squeezes Stop out → R-300 (halt). | spec | closed |
| R-240 | L01 ambiguous row | `Rows`, `Log`, cf. R-186 | V3 pass 2 @de56368: the AMBIGUOUS row reads only "or KE7QRS" — the kept candidate's callsign is missing visually and in the description ("half-filled circle, Ambiguous, or KE7QRS"); board: primary in `text/high` + " or QRF" in amber. `overnight/L01-log.png`. WP5 (`LogRowViewState` callsign) + WP2 if `LogRow` drops it. V4 pass 2 @ee4fd07: NOT fixed on device — the Log row still reads only "or KE7QRS" → R-322. WP5 d9d6bf0: see R-322 — fixed for real via rank. | design | fixed |
| R-241 | T02 Thread-Detail | `Thread-Detail`, FR-UI-4 | V3 pass 2: every INFERRED reasoning line reads "inherited (source transmission not recorded)" with no source-over link, and tapping does nothing — the fixture's inferred overs carry no `sourceId`; when a source exists the line must link to it, and the `overnight` fixture must include an inferred over with a real source. `overnight/T02-inherited-tap.png`. WP5 (+ fixture in `app/src/debug`). | spec | fixed |
| R-242 | L05 Log-Rejected | `Log-Rejected`, FR-ASR-5 | V3 pass 2: rows show only "REJECTED · SQUELCH TAIL" — no why-line (R-043's why comes only when the record carries a detail: the `overnight` fixture's rejection must carry one), DUR column blank, and tapping opens nothing — the rejected row must open the rejected detail state (F04) with the retained audio. `overnight/L05-rejected.png`. WP5 (+WP6's rejected detail route). WP5 (732b208): why-line and DUR column wired. Tap-through to F04 stays with WP6. WP6 79e4afd: rejected rows open the `Fail-Hallucination` detail state (`RejectedViewState`, header + transcript sections, audio reachable; no restore write path so the bottom bar renders nothing; the 4-of-6 controls checklist not built — data for 5 of 6 does not exist). | spec | fixed |
| R-243 | L02 Log-Filter | `Log-Filter`, FR-UI-3 | V3 pass 2: the `to` bound reads "—" on open; both bounds pre-fill with the data's extent. `overnight/L02-filter.png`. WP5. | design | fixed |
| R-244 | L01 @2x | `Rows`, guide §5, AC-63 | V3 pass 2: at font scale 2.0 badges (NEW / score chip) collapse to a sliver or vanish when the callsign wraps — the badge must wrap below the callsign, never clip. `overnight/L01-log@2x.png`. WP2 (`LogRow`). | spec | fixed |
| R-245 | L01 @2x column header | `Rows`, guide §5 | V3 pass 2: "STATION" wraps mid-word to "STATIO / N" at 2.0. `overnight/L01-log@2x.png`. WP2 (`ColumnHeaderRow`: `softWrap = false`, flexible width). | polish | fixed |
| R-246 | L03 Log-Partial | `Log-Partial`, FR-UI-1 | V3 pass 2: the Pass A partial's transcript is upright; the board italicises the whole provisional row ("Provisional must look provisional"). `pass-a-partial/L03.png`. WP5. WP5 (732b208): italics derive from `LogRowViewState.partial`. V4 pass 2 @ee4fd07: confirmed (`pass-a-partial/L03-partial-pass2.png`). | design | closed |
| R-247 | L04 Log-Empty (`empty`) | `Log-Empty`, FR-UI-1 | V3 pass 2: with no session at all the body is blank below the column headers — no chips, no "No overs yet" headline, no sentence. `empty/L04.png`. WP5. | design | fixed |
| R-248 | L04 Log-Empty (`first-session`) | `Log-Empty` | V3 pass 2: "Listening since 17:24." drops "on 145.230 and 146.960" (Now renders it for the same session), and the dimmed per-frequency chips are missing. `first-session/L04b.png`. WP5. | design | fixed |
| R-249 | L01 gap rows | `Rows` | V3 pass 2: durations render "52s" — the board writes "38 s" (space before the unit); apply through the shared duration formatter. `gap-call/L01-mid4.png`. WP5. | polish | fixed |
| R-250 | DG03 Sessions, DG04 Session @2x | `Sessions`, guide §11.3 | V6 pass 2 @59e42c4: setting font scale 2.0 with a gap row on screen ANRs the app (input dispatch timeout, never recovers) — `SessionsScreens.drawGapHatch` loops `while (x < size.width) { x += stripeWidth * 2 }` with `stripeWidth = size.width / 3f`, so a zero-width canvas never advances. Guard zero/negative sizes and use a fixed stripe pitch. `stations-14-nights/DG04-session-pass2@2x.png`. WP10. V6 pass 3 @1b3f645: confirmed, no ANR at 2.0 (`stations-14-nights/DG04-anr-check-pass3.png`). | halt | closed |
| R-251 | CF03 Settings-Storage @2x | `Settings-Storage`, guide §6.3 | V6 pass 2: the legend row and the GB-budget chip row now clip at the right edge ("Reco", "U") with no reflow and no horizontal scroll (swipe does nothing) — use WP2's scrolling `FilterChipRow` and a wrapping legend. `overnight/CF03-settings-storage-pass2@2x.png`. WP10. V6 pass 3 @1b3f645: legend now wraps; the GB chip row still clips and does not scroll → R-291. | spec | partial |
| R-252 | F20 Fail-Migration @2x | `Fail-Migration`, guide §5 | V6 pass 2: on first layout the fixed bottom bar still clips the last bullet and "Save a diagnostic bundle first" overlaps the paragraph; only after a manual scroll is the padding right — the initial scroll state does not reserve the bar height (measure the bar before the first frame, or reserve its max height). `migration-failed/F20-pass2@2x.png`. WP11b. V6 pass 3 @1b3f645: NOT fixed — first-layout overlap unchanged → R-292. Close-out: tracked by R-292 (fixed by WP11b fc4dc8e, height-constrained scaffold); V6 pass 4 confirms both. | spec | fixed |
| R-253 | CF01 Settings | `Settings` | V6 pass 2: R-131 was marked fixed but the rows still have no 18px leading icon and the Tier sub-line lacks "· this phone's best · what it does not know". Use WP2's `NavRow(icon = …)`. `overnight/CF01-settings-pass2.png`. WP10. V6 pass 3 @1b3f645: confirmed. | design | closed |
| R-254 | F8 Fail-Backlog | `Fail-Backlog` | V6 pass 2: live-bar "112 behind" and Waiting/Capture/In-the-log rows are right; the Rate row ("Band N overs/min · Pass B N/min") and the 30-minute queue chart are still absent. `backlog/F8-banner-pass2.png`. WP11b. V6 pass 3 @1b3f645: confirmed (`backlog/F8-banner-pass3.png`). | design | closed |
| R-260 | Now earlier-nights rows @2x | `Main`, `Now-Idle`, guide §11.3 | V7 @b94bb5e: the trailing " · 1 gap" renders one character per line in a ~16 px column at font scale 2.0 (same collapsed-column class as R-150). `overnight/N01-now@2x.png`. WP4. V2 pass 3 @8cbb13a: confirmed (`overnight/N01-now@2x-pass3-v2.png`). | spec | closed |
| R-261 | Detail correction sheet (D08–D10) | `Detail-Correct-*`, guide §11.2 | V7 @b94bb5e: the detail's "Back to Log" header button stays focusable/clickable behind the sheet scrim (collapsed to a 10 px hit box) and receives focus from taps inside the sheet — the scrim must block traversal (`Modifier.semantics { invisibleToUser() }` / `clearAndSetSemantics` on the content beneath, or a `Dialog`/focus-trapped `Sheet`). `overnight/D10-focus-bug-check@2x.png`. WP2 (`Sheet` scrim) + WP6. WP2 (548b229): `Modifier.clearedWhileOverlaid`; WP7 (7312785) applied it to Search's filters sheet. WP6's correction sheet still pending. WP6 a9c24c6: `clearedWhileOverlaid` on the detail behind the correction sheet — both halves landed. V4 pass 2 @ee4fd07: confirmed via dump with the sheet open (`overnight/D08-sheet-open-dump-pass2.png`); Search half confirmed by V5 pass 3. | spec | closed |
| R-262 | CF04 Settings-Assets (and every scrolling destination) | `Settings-Assets`, guide §6.6 | V7 @b94bb5e: the pinned live bar overlays the bottom of the scrolling list at every position — no bottom content padding is reserved; the host must pad every destination's content by the live bar height while it shows. `model-missing/CF04-assets-scrolled@2x.png`. WP3 (host padding) + WP10. | spec | fixed |
| R-263 | Capture-Status Radio row | `Capture-Status`, guide §9 | V7 @b94bb5e: "No rig configured — FR-RIG is not built yet" leaks a spec id into operator copy; say "no radio support in this build yet". `thermal/N04-capture-status@grey.png`. WP4. V2 pass 3 @8cbb13a: confirmed. | design | closed |
| R-264 | S07 reachability | plan §E, R-227 | V7 @b94bb5e: the README recipe launches `SetupActivity` directly but it is `exported=false` (SecurityException); via `MainActivity` the `setup-verified` scenario opens S12, whose Level row is not clickable, so S07 is unreachable. Give `setup-verified` a `--es step LEVEL` variant (or a `setup-level` scenario seeding "level not yet measured") and fix the README recipe to go through `MainActivity` + `EXTRA_STEP`. WP9. | process | closed |
| R-265 | S12 Setup-Done rows | `Setup-Done`, guide §11.2, FR-A11Y-2 | V7 @b94bb5e: the five status rows are plain non-focusable `Text`s — TalkBack never reads "Input, USB Audio Device, verified"; each row needs a merged, focusable semantics node (WP2's `KeyValueRow` should provide it — check it does, else fix there). `setup-verified/S12-ready-via-main@2x-retry.png`. WP9 (+WP2). WP9 6bf48c7: rows sit on the native focusable `KeyValueRow`; status text folds into its merge, the Fix action stays a separate button stop; `ReadyScreenTest` 7/7 green, main gate green again at 5f7077d. V1 pass 4 @a038128: Overnight/Radio/Model rows are one merged node with the action separate (correct); Input and Level rows still expose "verified"/"in band" as a separate unmerged node → R-342 (halt). WP9 d39131a: covered by R-342; V1 pass 5 confirms. | halt | fixed |
| R-266 | Now CAN GET BETTER item | `Main`, guide §9 | V7 @b94bb5e: "captured at T1 tier" — raw tier token in Now's digest item while Improve says "tier 1" correctly; use the shared tier label. `field-tier1/R01-improve@2x.png`. WP4. V2 pass 3 @8cbb13a: confirmed (`field-tier1/N01-now-tier1-pass3b.png`). | design | closed |
| R-267 | CF04 Settings-Assets tokens row | `Settings-Assets`, guide §9 | V7 @b94bb5e: the "Whisper tiny.en — tokens" sub-line renders a multi-paragraph maintainer note (SHA-1 vs SHA-256, HuggingFace behaviour, "checked 2026-09-07") verbatim — wrong audience; the row shows size · checksum prefix · tier only, and the note moves to a developer comment. `model-missing/CF04-assets-scrolled@2x.png`. WP10. V6 pass 3 @1b3f645: confirmed (`model-missing/CF04-assets-pass3.png`). | design | closed |
| R-270 | ST02 Recent overs rows | `Rows`, guide §6.5, cf. R-205 | V5 pass 2 @8d1456f: Station detail's recent-overs rows still wrap the time/frequency columns mid-value ("05:37:0/0") — the screen hand-rolls its row instead of using WP2's fixed `LogRow`/column widths. `stations-14-nights/ST02-detail-pass2.png`. WP8. WP8 d39c056: remembered time/freq column widths, `softWrap = false`. V5 pass 3 @0455401: confirmed at 1.0 and 2.0 (`stations-14-nights/ST02-detail-pass3.png`, `-pass3@2x.png`). | design | closed |
| R-271 | ST03 Hour × day axis | `Station-Pattern` | V5 pass 2: the hour labels "00 06 12 18" wrap to two lines each at default scale. `stations-14-nights/ST03-hourxday-pass2.png`. WP2 (`DayOfWeekGrid` axis `softWrap = false`) / WP8. WP8 d39c056: confirmed the axis wrap lives inside WP2's `DayOfWeekGrid` → WP2. WP2 ba12e71: `hourLabelStride(fontScale)` doubles the stride at ≥2.0, labels `softWrap = false` unbounded width, both orientations; `ActivityPatternChartTest` R_271. | polish | fixed |
| R-272 | ST04 Split | `Station-Identity`, `Fail-Cluster` | V5 pass 2: tapping `Split` shows a bare "Loading…" forever — `SplitSubScreen` treats `voiceSplitCandidates == null` as both "loading" and "nothing to split", with no header, no back and no empty state; logcat floods with `SQLiteConnectionPool leaked` while waiting (a DB opened per call and never closed). Render a real empty state ("one cluster, nothing to split") with back, and reuse one `OrtDatabase`. `stations-14-nights/ST04-split-stuck.png`. WP8. WP8 d39c056: split empty state distinguished from loading; one `SharedDatabase` per process; `StationDetailContentTest R_272`. V5 pass 3 @0455401: Split opens instantly to a real "One cluster, nothing to split" state, no leak (`stations-14-nights/ST04-split-pass3.png`). The multi-voice branch is unverifiable on device — no scenario seeds a `VoiceprintEntity`; WP4 to seed a two-voiceprint station in `stations-14-nights`. WP4 95267d4: WA7HJR bound to two real `VoiceprintEntity` clusters in `stations-14-nights` (`VoiceSplitFixtureTest`); the multi-voice Split branch is now reachable — V5 to confirm. | halt | fixed |
| R-273 | FQ03 subtitle | `Frequency-Change` | V5 pass 2: "Tonight · 20 overs where the usual is 2" lacks the departure window ("Tonight, 02:00–04:00 · …"). `frequency-change/FQ03-change.png`. WP8. WP8 d39c056: real departure-window subtitle. V5 pass 3 @0455401: confirmed (`frequency-change/FQ03-change-pass3.png`). | design | closed |
| R-274 | FQ03 chart | `Frequency-Change` | V5 pass 2: a bare sparkline instead of the dual-series comparison — tonight's amber bars over the usual green line across a labelled 22:00–06:00 axis, with the "tonight / usual, N nights" legend. `frequency-change/FQ03-change.png`. WP8 (+WP2 if a comparison chart component is warranted). WP8 d39c056: dual-series chart with axis and legend. V5 pass 3 @0455401: confirmed. | design | closed |
| R-275 | FQ03 closing paragraph | `Frequency-Change` | V5 pass 2: only the first sentence renders; the board continues with the explanation, the digest note and the "will not change what usual means" sentence. `frequency-change/FQ03-change.png`. WP8. WP8 d39c056: full three-part closing paragraph. V5 pass 3 @0455401: confirmed. | design | closed |
| R-276 | FQ03 "The N overs" | `Frequency-Change` | V5 pass 2: the action is not clickable — it must open the frequency's over list (LOG filtered to the frequency and window via the host callback). `frequency-change/FQ03-change.png`. WP8 (+WP3 route). WP8 d39c056: `onOpenOvers(Long, TimeWindow)` wired; WP3 routes it to the filtered Log. WP5 c7d5d5a: `LogContent(initialFilter)` seeds selection and quick filter; count line honest for a window. WP3 round 9 routes the host. WP3 ddd515b: `NavHostNavState.openLogFilteredByFrequency` → Log with `initialFilter` (frequency chip selected, count line honest; real-Activity smoke test through FQ01→FQ02→FQ03→Log). System back reopens FQ02, not FQ03 — WP8 adding `FrequencyDetailContent` initial-view param, WP3 round 10 wires it. WP8 (this round): `FrequencyDetailContent(initialView: FrequencyDetailView = FrequencyDetailView.Detail)` — `FrequencyDetailView` (`ui/data/StationsAndFrequencies.kt`) is `{ Detail, Change }`; passing `FrequencyDetailView.Change` seeds the sub-screen state directly on `Frequency-Change`, its own back still returning to FQ02 as today. WP3 round 10 to wire the value on the reopen from Log. WP3 bf5c97f: system back from the filtered Log lands on FQ03 (`frequencyInitialView = Change`); smoke test covers both directions. | spec | fixed |
| R-277 | ST01 filter chips @2x | `Stations`, guide §6.3 | V5 pass 2: the Tonight / All time / Named / Unidentified chip row clips ("Uni") and does not scroll at font scale 2.0 — use WP2's scrolling `FilterChipRow` as ST03 does. `stations-14-nights/ST01-unidentified-chip-clipped@2x.png`. WP8. WP8 d39c056: `FilterChipRow` (already) + `fillMaxWidth()`. V5 pass 3 @0455401: confirmed (`stations-14-nights/ST01-pass3-afterswipe@2x.png`). | design | closed |
| R-280 | S02b, S03, S05 timed out, S06 | `Setup-*` | V1 pass 3 @a272464: the ghost, semi-transparent duplicate of the bottom bar's secondary action still renders below the status bar on every two-button Setup screen (R-220's fix removed the measurement loop but not the ghost) — find what draws it: likely `SetupScaffold` composing the bottom bar twice (a `Box` with the bar both as scaffold slot and in the content), or a `TextAction` drawn once for measurement. Prove with a test that the secondary action's text appears exactly once in the tree. `setup/S02b-mic-denied-pass3-recheck.png`, `setup/S03-notify@2x.png`. WP9. WP9 8a08a8f: no composable duplicates a secondary action (five "exactly once" tests across S02b/S03/S05/S06); reproduces only after backgrounding into Settings and back — likely a window-transition artefact. V1 pass 4 to recapture on a cold navigation before any further work. V1 pass 4 @a038128: refuted — the ghost reproduces on a cold navigation with one node in the tree → R-340. | design | open |
| R-281 | S03 Setup-Notify @2x | `Setup-Notify`, AC-63 | V1 pass 3: at 2.0 the closing paragraph's last line and the preview card's third line are clipped by the scroll viewport above the fixed bar. `setup/S03-notify@2x.png`. WP9. WP9 8a08a8f: `SetupScaffold` scroll already covered it; fontScale-2.0 regression test added. V1 pass 4 @a038128: NOT fixed on device — clipped again on first composition at 2.0 → R-341. WP9 d39131a: covered by R-341; V1 pass 5 confirms. | spec | fixed |
| R-282 | S06 Setup-Route-Mismatch @2x | `Setup-Route-Mismatch`, guide §5 | V1 pass 3: the mono descriptor line is cut mid-word by the bar at 2.0. `setup/S06-route-mismatch@2x.png`. WP9. WP9 8a08a8f: same scaffold; fontScale-2.0 regression test added. V1 pass 4 @a038128: NOT fixed on device → R-341. WP9 d39131a: covered by R-341; V1 pass 5 confirms. | spec | fixed |
| R-283 | S08 Setup-Battery @2x | `Setup-Battery`, guide §5 | V1 pass 3: numbered step 1 is clipped by the viewport above the bar at 2.0 — R-123's fix covered S01/S04 only; every Setup screen must use the same scaffold padding. `setup-verified/S08-battery@2x.png`. WP9. WP9 8a08a8f: same scaffold; fontScale-2.0 regression test added. V1 pass 4 @a038128: NOT fixed on device → R-341. WP9 d39131a: covered by R-341; V1 pass 5 confirms. | spec | fixed |
| R-284 | S05 Setup-Verify while listening | `Setup-Verify` | V1 pass 3: during the 30 s signal check the board shows disabled `Continue` plus the `Choose a different input` link; the build shows only the disabled button until the timeout. `setup/S05-verify-listening-pass3b.png`. WP9. WP9 8a08a8f: muted "Choose a different input" link during the listening check (`textMuted`). V1 pass 4 @a038128: confirmed (`setup/S05-verify-listening-pass4.png`). | design | closed |
| R-285 | S09–S11 reachability | `Setup-Rig*`, plan §E | V1 pass 3: no sanctioned path reaches the radio steps — the linear flow stalls at S05 on the AVD, `setup-verified` resolves `radioChoice = NONE` and skips RADIO, S12's Radio row has no `Fix`/`Change radio` target. Add a `setup-radio` scenario resuming at RADIO and a `Change` action on S12's Radio row. WP9. WP9 8a08a8f: `setup-radio` scenario resumes at S09; S12 Radio row gains `Change` → `SetupActivity.onChangeRadio`. V1 pass 4 @a038128: reachability confirmed (`setup-radio/S09-pass4.png`, `S10-pass4.png`, `S12-radio-change-pass4.png`); exercising it surfaced R-344. | process | closed |
| R-290 | R03 Improve-Running (`field-tier1`) | `Improve-Running`, FR-REP, cf. R-143 | V6 pass 3 @1b3f645: the real `RealImproveRunner` reaches R03 with genuine cadence ("Improving · All groups · 0 of 12", Pause/Cancel) and then **crashes the app** on the first item: `IllegalStateException: expected retained audio at …/audio/scenario-field-tier1/scenario-field-tier1-tx1.flac` at `FlacSegmentAudioProvider.forItem` — 100% reproducible, advances one item per relaunch. Two defects: the reprocess engine must fail the item (mark failed, keep going, surface in R03/R04 and `pass-failed`) instead of throwing uncaught — a real device with retention-deleted audio hits the same crash; and the `field-tier1` fixture must seed real retained-audio FLAC files so R04 is reachable. `field-tier1/R01-improve-pass3.png`, `field-tier1/R03-immediate-pass3.png`, `field-tier1/R03-crash-pass3.png`. WP11d (`:pipeline/reprocess`, `FlacSegmentAudioProvider`) + WP4 (fixture, `app/src/debug`). WP11d c565091: `SafePass` converts any pass exception to `Errored` → item `FAILED` with the reason in `ReprocessStatus.Summary.failureReasons`; run continues (`R_290` test). Fixture half (real FLAC) still with WP4; live-capture loop gets the same wrap (WP11c, `R_290_live`). WP11c fde9d8e: the live processing loop's pass is wrapped in the same `SafePass` (`R_290_live`); only the fixture half (real FLAC, WP4) remains. WP4 95267d4: `field-tier1` writes real decodable retained audio for all twelve overs (`ScenarioFixtures.writeAudioFixture`, same codec `FlacSegmentAudioProvider` reads; `FieldTier1AudioTest`). All three halves landed — engine, live loop, fixture. | halt | fixed |
| R-291 | CF03 Settings-Storage @2x | `Settings-Storage`, guide §6.3, cf. R-251 | V6 pass 3 @1b3f645: the GB-budget chip row ("10 GB / 20 GB / 30 GB / 60 GB / Unlimited") still clips at the right edge ("Unli…") at font scale 2.0 and does not scroll on swipe; use WP2's scrolling `FilterChipRow` as ST01/ST03 do. `overnight/CF03-storage-pass3-2x.png`, `overnight/CF03-storage-pass3-2x-afterswipe.png`. WP10. WP10 dec5c01: `FilterChipRow` in `SettingsStorageScreen` was missing `fillMaxWidth()` (same root as R-277); measured-width test. | spec | fixed |
| R-292 | F20 Fail-Migration @2x | `Fail-Migration`, guide §5, cf. R-252 | V6 pass 3 @1b3f645: on first composition at 2.0 the fixed bottom bar ("Rebuild now and continue") still overlaps the last bullet's sub-line and "Save a diagnostic bundle first" overlaps the closing paragraph; only a full scroll to the bottom clears it — the initial scroll state does not reserve the bar's height (measure the bar before the first frame or reserve its max height as content padding). `migration-failed/F20-pass3-2x-initial.png`, `-afterscroll.png`, `-fullscroll.png`. WP11b. WP11b fc4dc8e: `FailureActionBarScaffold` constrains the content slot height to screen − bar (F1, F6, F20, F21); `R_292` cold-launch test at 2.0. | spec | fixed |
| R-300 | N04 Capture-Status with a banner @2x | `Capture-Status`, `Fail-Storage`, FR-UI-7, guide §11 | V2 pass 3 @8cbb13a: at font scale 2.0 the `Fail-Storage` banner plus its "How this unfolded" card render as a fixed top-of-content block that consumes nearly the whole viewport; `capture-status-title`, the "Since … heartbeat" line and `capture-status-stop` are squeezed into a sliver behind the pinned live bar and the Stop control's tap target collapses to `[0,0][0,0]` in the accessibility tree (fresh navigation, not a stale scroll). The primary Stop becomes untappable at this text scale; only the notification's Stop works. The banner block must scroll with the content (or cap its height and scroll internally) and Stop must stay reachable — pin it or keep the content column scrollable beneath a bounded banner. `storage-warn/N04-capture-status-banner@2x-pass3.png`, `storage-warn/capture-status-banner-pass3-fresh@2x.png`. WP11b (`FailureHost` banner block bounded/scrolling) + WP4 (`CaptureStatusContent` scrollable, Stop reachable). WP11b 2111abc: `FailureHost` caps every banner to 40% of the viewport (scrolls inside), `Fail-Storage`'s "How this unfolded" card collapses behind Details at ≥1.3 scale; `contentTopPadding` contract unchanged; `R_300` test composes the real `CaptureStatusScreen` at 2.0 and asserts Stop has non-zero bounds. `CaptureStatusScreen` already scrolls with title/Stop first (WP4 half needs no change). | halt | fixed |
| R-301 | N04 Capture-Status Overs row | `Capture-Status`, guide §9 | V2 pass 3 @8cbb13a: sub-line reads "1 rejected · 0 failed · 1 gaps" — "1 gap"; use the shared plural helper (same class as R-141/R-190/R-212). `overnight-live/N04-capture-status-pass3.png`. WP4. WP4 c4b17bc: Overs row uses the shared `Plurals.count` helper. | polish | fixed |
| R-302 | N04 Capture-Status, N02 Now-Idle after stop | `Fail-Level`, FR-RUN | V2 pass 3 @8cbb13a: after a real capture is stopped through `capture-status-stop` → confirm, the F03 "Too quiet since HH:MM:SS …" banner keeps showing on the post-stop Capture-Status and on Now-Idle although the title reads "Not capturing" — `LevelStatus` (and by inspection `InputStatus`) are not reset when `RealCaptureService` stops, so a stale level problem reads as current. Clear the level/input holders on stop (`Idle`/`NotCapturing` state), and the banner must never show for a session that is not running. `empty/N04-after-stop-pass3.png`, `empty/N02-idle-after-stop-pass3.png`. WP11c (`RealCaptureService` stop path, holders). WP11c fde9d8e: `stopCaptureInternal` resets `LevelStatus`/`InputStatus` on every session-ending path; test `R_302_stop_resets_level_and_input_status_to_not_capturing`. The belt-and-braces mapper check (WP11b's `FailureMapper`) not done. | spec | fixed |
| R-310 | ST03 Station-Pattern, Hour × day legend @2x | `Station-Pattern`, guide §11.3, AC-63 | V5 pass 3 @0455401: at font scale 2.0 the Hour × day legend's "not listening" item collapses into a one-letter-per-line column down the right edge (same collapsed-flex-column class as R-150/R-260); the By-hour mode's legend row is correct at the same scale, so the defect is the Hour × day chart's own legend layout. `stations-14-nights/ST03-hourxday-pass3@2x.png` (`ST03-byhour-pass3@2x.png` as control). WP2 (`DayOfWeekGrid` legend). WP2 c87dab0: `DayOfWeekGridLegend` is a `FlowRow` (items reflow as whole units), labels `maxLines = 1, softWrap = false`; test `R_310` at 2.0. | spec | fixed |
| R-311 | FQ01 Frequencies | `Frequencies` | V5 pass 3 @0455401: the list's closing caption ("Sparkline is overs per night, 14 nights. Hatched nights: not listening on that frequency.") never renders — absent from the screenshot and the dump on a two-row list with room below. `stations-14-nights/FQ01-pass3.png`. WP8. WP8 ff2d628: trailing `SparklineCaption` item in `FrequenciesListScreen`; test `R_311`. | polish | fixed |
| R-312 | FQ01 Frequencies row | `Frequencies`, cf. R-212 | V5 pass 3 @0455401 (`frequency-change`): "1 overs tonight · 1 stations" — the row summary is not run through the shared plural helper (FQ02's REGULARS list is correct). `frequency-change/FQ01-pass3.png`. WP8. WP8 ff2d628: row summary and its description via the shared `pluralize()`; test `R_312`. | design | fixed |
| R-340 | S02b, S03, S05, S06, S09, S11 bottom bar | `Setup-*`, cf. R-220/R-280 | V1 pass 4 @a038128: the ghost, semi-transparent duplicate of the bottom bar's secondary action reproduces on a pure cold navigation (fresh `pm clear`, no backgrounding) and is pixel-identical after backgrounding — WP9's window-transition hypothesis is refuted. The accessibility tree carries exactly one node for the action text every time, so it is a rendering/compositing defect, not a duplicate composable; it appears with a single bottom link (S09) and with `Reconnect`/`Change radio` (S11), and its text always mirrors the current secondary action (re-rendered live). Look for an animated/fading copy (`AnimatedContent`/`Crossfade`/`AnimatedVisibility` leaving an alpha layer), a `graphicsLayer(alpha)` plus shadow, or a text drawn in both the bar and its scrim. `setup/S02b-mic-denied-cold-pass4.png`, `setup/S02b-mic-denied-after-backgrounding-pass4.png`, `setup/S03-notify-cold-pass4.png`, `setup/S05-verify-timedout-pass4.png`, `setup/S06-route-mismatch-pass4.png`, `setup-radio/S09-pass4.png`, `rig-lost/S11-stale-check-pass4.png`. WP9. WP9 d39131a: confirmed not a duplicate composable/window/node; ghosts only when `bottomActions` has a second `Box { TextAction }` child → `TextAction` modifier chain in `Controls.kt`. WP2 bisecting on 5556. | design | open |
| R-341 | S01, S03, S06, S08, S12 @2x | `Setup-Welcome`, `Setup-Notify`, `Setup-Route-Mismatch`, `Setup-Battery`, `Setup-Done`, guide §5 | V1 pass 4 @a038128: at font scale 2.0 every Setup screen's scrollable content is clipped by the fixed bottom bar on the first composition, before any scroll — reopening R-123, R-281, R-282, R-283 (each marked fixed by a regression test that evidently does not reproduce the first-frame layout). One manual scroll reveals everything, so the scaffold's bottom padding is only right after a remeasure. Same class as R-292: constrain the content slot's measured height to viewport − bar (WP11b's `FailureActionBarScaffold` fix at b40f659 is the pattern), not padding; the test must assert on the very first frame at 2.0 with content long enough to overflow. `setup/S01-welcome-pass4@2x.png` vs `setup/S01-2x-afterscroll.png`, `setup/S03-notify-pass4@2x.png`, `setup/S06-route-mismatch-pass4@2x.png`, `setup-verified/S08-battery-pass4@2x.png`, `setup-verified/S12-ready-pass4@2x.png`. WP9 (`SetupScaffold`). WP9 d39131a: `SetupScaffold` on `SubcomposeLayout` with content `maxHeight = screen − header − bar` (R-292 pattern). Robolectric cannot observe the pre-settle first frame, so V1 pass 5 must confirm on device. | spec | fixed |
| R-342 | S12 Setup-Done, Input and Level rows | `Setup-Done`, guide §11.2, cf. R-265 | V1 pass 4 @a038128: R-265's fix covers Overnight/Radio/Model (one merged node, action separate — correct) but the Input and Level rows' trailing status ("verified", "in band") is still a separate unmerged node with its own description while the row reads only "Input, USB Audio Device" — TalkBack hits it as a contextless second stop. Dump: `content-desc="Input, USB Audio Device"` at `[109,401][1027,517]` and `content-desc="verified"` at `[891,417][1027,451]` as its own node. Fold the status into the row's description the way the other three rows do (the same `statusText` path), or give `KeyValueRow` a merging trailing-status slot if those rows use a different one. `setup-verified/S12-done-pass4.png`. WP9 (+WP2 if `KeyValueRow` lacks the slot). WP9 d39131a: `clearAndSetSemantics` on the row's outer node (a lone descendant `contentDescription` merged under Robolectric but not in the real `AccessibilityNodeInfo` tree); Input/Level re-dumped on device as one node. Radio row's status+action combination not re-dumped (no scenario reaches it). | halt | fixed |
| R-343 | S09 Setup-Rig title | `Setup-Rig` | V1 pass 4 @a038128: the board's title row is "Radio" with an 11px "optional" label beside it, baseline-aligned; the built screen shows only "Radio". `setup-radio/S09-pass4.png`. WP9. WP9 d39131a: `SetupScaffold.titleOptional` renders the "optional" tag beside "Radio". | design | fixed |
| R-344 | S09 "No radio — I will enter the frequency" | `Setup-Rig`, `Flow-Setup`, FR-CAP | V1 pass 4 @a038128: tapping the row whose label says "No radio — I will enter the frequency" advances straight to S12 with the Radio row reading "No radio · frequency by hand" and no number — setup completes having never collected a frequency, so every over of a session started this way would carry no frequency (constitution: never silently lose a fact). The real frequency-entry screen (honest no-rig banner, `Frequency (MHz)` field, "Enter the frequency instead" / Back) is reachable only via the second row, "Another rig with a serial CAT interface", which is the wrong row for it. Reproduced 3×. Route the third row to the frequency-entry step and require a value before S12 (S12's Radio row then shows the entered frequency); the second row keeps the CAT/USB path. `setup-radio/S09-no-radio-row-skips-to-ready-BUG-pass4.png`, `setup-radio/S10-pass4.png`. WP9 (`SetupActivity` S09 routing). WP9 d39131a: the third S09 row routes through `RADIO_USB` (frequency entry); `onEnterFrequency` refuses null and the button stays disabled until a valid MHz value; real-Activity test S09 → row → field → S12. | halt | fixed |
| R-320 | D05 Detail-Why, section 1 | `Detail-Why`, FR-UI-8, cf. R-180 | V4 pass 2 @ee4fd07: `Full lattice` opens D05 with real prior bars and the survivors list, but the phonetic-lattice section still reads "Per-slot detail (each unit's score and kept alternate) is not recorded yet." — the board's namesake per-slot grid (unit / score / kept alternate) is absent because `PhoneticLatticeEntity.unitsBlob` is opaque and no per-slot detail is persisted. Decision: `:lexicon`'s resolver must emit per-slot detail (unit, score, kept alternate, and the transcript character span — the same span R-182 needs) and `:data` must persist it (schema v5) so D05 and the D01/D03 highlight can render it; WP6 renders once it exists. `overnight/D05-why-pass2.png`. :data agent (schema + DAO) + :lexicon resolver (WP11d's agent owns `:pipeline`; resolver is `:lexicon`) + WP6. lexicon 250dfbb: `SlotDetail(index, unit, score, keptAlternate?, charStart?, charEnd?)` on `CallsignCandidate.slotDetails` (runner-up per slot, null when a slot has one alternative). :data persists (`lattice_slot`, schema v5), then WP6 renders D05. | halt | partial |
| R-321 | D01/D03/D11 after Undo all | `Detail-Propagated`, constitution I, cf. R-189 | V4 pass 2 @ee4fd07: R-189's patch hardcodes INFERRED-with-null-confidence, so `Undo all` after a Tier-A pick on the AMBIGUOUS over leaves the Log row reading "Unknown … corrected" while the detail shows a hollow-ring marker, a leftover CORRECTED badge and "Not heard in this over. Matched by voice." with no confidence — neither the original AMBIGUOUS chooser nor a coherent INFERRED state; the same after a typed correction on the CONFIRMED over (W7NPC). Log and detail disagree on the same transmission. Undo must restore the recorded prior attribution (state, callsign, confidence, source) from the correction record — if `CorrectionEntity` does not store the prior, `:data` must add it — and clear the CORRECTED badge. `overnight/D03-after-undo-pass2.png`, `overnight/L01-after-undo-ambiguous-pass2.png`, `overnight/D01-after-undo-confirmed-pass2.png`. WP6 (+`:data` if the prior is not recorded). WP6 dc271b5: root cause confirmed — `applyCorrectedAttribution` always writes INFERRED/null and `undoAll` reverts through the same write; `CorrectionEntity` stores only the previous callsign. :data adding `previousAttribution*` columns + `CorrectionDao.restoreAttribution` (schema v5); WP6 calls it next. | halt | partial |
| R-322 | L01 Log, AMBIGUOUS row | `Rows`, `Log`, cf. R-240 | V4 pass 2 @ee4fd07: the Log row still reads only "or KE7QRS" in amber with the kept candidate absent visually and in the description ("13:04:56, 145.230, Ambiguous, or KE7QRS, …"), reproduced on two Log loads; D03's header is right, so WP5's `keptCandidateFor` → `LogRowViewState.callsign` is not reaching `LogRow` on the device (test passes against a fixture shape the real mapper does not produce?). `overnight/L01-log-pass2.png`, `overnight/L01-log-real-pass2.png`. WP5 (+WP2 if `LogRow` drops it). WP5 d9d6bf0: `keptCandidateFor`/`alternateFor` read rank 0/1 instead of a `selected` flag the resolver never sets on AMBIGUOUS; `R_322` drives the real `overnight` fixture through `LogPolling` → `LogRow`. | design | fixed |
| R-323 | D05 Detail-Why priors @2x | `Detail-Why`, guide §4, AC-63 | V4 pass 2 @ee4fd07: at font scale 2.0 the prior rows wrap mid-word ("Propagatio / n", "Callsign / history") with the bar and value squeezed beside the fragment instead of reflowing beneath the label. `overnight/D05-fontscale2-pass2.png`. WP6. WP6 dc271b5: `PriorBar` splits label / meter / value and reflows the bar beneath the label at ≥1.3 scale; label/value `softWrap = false`; tests `R_323` on the unmerged tree. | design | fixed |
