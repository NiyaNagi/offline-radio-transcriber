package org.ort.app.ui.setup

/** S09's three options (`Setup-Rig.dc.html`) — a closed set, never a free string (guide §6.11). */
public enum class RadioChoice {
    TH_D75A,
    OTHER_CAT_RIG,
    NONE,
}
